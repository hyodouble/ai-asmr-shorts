---
name: ai-asmr-shorts
description: 상품/쿠팡 없이 AI로 ASMR 영상을 만든다. 수면용 1시간 롱폼(클립 루프)과 숏폼 둘 다 대응하고, 기획·씬 프롬프트·렌더 검수·롱폼 조립·다국어 배포 메타데이터까지 생성한다.
---

# AI ASMR Shorts

Claude + **Veo 3.1 Fast (Google Flow)** 로 니치 하나에 고정된 AI ASMR 영상을
링크나 상품 없이 컨셉 한 줄로 끝까지 만든다.

주 배포물은 **수면용 1시간 롱폼**(유니크 클립을 이어붙인 마스터를 반복). 숏폼은 같은 클립 재활용해서 나중에 낸다.

## 도구 / 계정 (실행 전 확인)

- 생성: Google Flow — https://labs.google/fx/tools/flow
- 계정: `hoohihi123123@gmail.com` (Google AI Pro). 브라우저 첫 프로필은 무료 티어라 Veo가 안 나온다.
  로그인 계정을 매번 확인할 것
- 모델: Veo 3.1 Fast (오디오 포함 생성). 화질이 부족한 컷만 Fast 아닌 Veo 3.1로 재생성
- 조립: ffmpeg (5단계). 없으면 `winget install Gyan.FFmpeg`
- Veo가 오디오를 같이 만들기 때문에 별도 SFX 라이브러리나 오디오 편집 단계는 두지 않는다


## 0단계 — 니치 고정 (최초 1회만)

여러 트리거를 섞지 않는다. 채널당 니치 하나가 알고리즘 성과가 더 좋다
(단일 포맷 채널이 8개 포맷 섞은 채널을 이긴다 — 알고리즘이 시청자를 한 트리거 중심으로 모아줌).

확정 니치는 `niche.md`에 기록하고 이후 모든 영상에 고정 적용한다.

## 1단계 — 컨셉 + 출력 포맷 입력

사용자는 한 줄만 준다: "오늘은 [소재] 자른다".
이어서 출력 포맷을 정한다.

- `롱폼` — 수면용 1시간 (기본값)
- `숏폼` — 60초 이하

상품 링크나 실물 구매 불필요.

## 2단계 — 장면 기획

포맷에 따라 파라미터가 다르다. 섞지 말 것.

| 항목 | 숏폼 | 수면 롱폼 |
|---|---|---|
| 씬 길이 | 2~3초 | 6~10초 |
| 컷 속도 | 빠르게 | 느리게, 급전환 금지 |
| 사운드 | 임팩트 강조 | 피크 억제, 레벨 균일 |
| 목표 지표 | 완주율 75%+ | 평균 시청 지속시간 |
| 유니크 클립 수 | 3~6개 | 20~40개 |

공통 규칙:

- 보이스오버 없음, 배경음악 없음. 오디오는 커팅/타격/붓기 사운드 하나로만 간다.
  오디오가 시청 이유의 70%. 여기 대충 하면 전체가 죽는다.
- 클로즈업 + 슬로우모션 위주. 실사 불가능한 스케일(거대 블레이드, 240fps 낙하 등)도 AI라 가능 — 적극 활용.
- 롱폼은 자다 깨게 만들면 실패다. 갑작스러운 밝기 변화, 화면 전체 플래시, 큰 파열음 금지.

## 3단계 — 씬별 프롬프트 자동 생성

장면마다 (영상 프롬프트 + 매칭되는 SFX 설명)을 클로드가 작성. 사용자는 승인만 한다.

롱폼은 클립 20~40개가 필요하니 소재 변주(색·크기·절단 각도·낙하 높이)를 표로 뽑아 한 번에 생성한다.
같은 프롬프트 반복 생성은 금지 — 루프 티가 난다.

### Veo 프롬프트 작성 규칙

프롬프트는 영어로 쓴다. 아래 6요소를 한 문단에 넣는다.

1. 샷 — `Extreme close-up, macro lens, static locked-off camera`
2. 피사체와 물성 — 무엇을 무엇으로 자르는지, 재질을 명시 (`translucent glass strawberry`)
3. 동작 — 느리게, 한 동작만 (`slowly slicing straight down through`)
4. **오디오 — 반드시 별도 문장으로.** `Audio: crisp crystalline crack, then a soft ringing chime. No music, no voice.`
   Veo는 이 문장을 보고 소리를 만든다. 빼면 무음이거나 엉뚱한 소리가 붙는다
5. 조명/속도 — `soft diffused studio light, slow motion`
6. 금지 — `no text, no on-screen captions, no people, no speech`

씬 변주는 재질·색·조명·절단 각도만 바꾸고 1·4·6번은 고정한다. 그래야 루프에서 톤이 안 튄다.


## 4단계 — 렌더 검수 (승인 전 필수 체크)

- 손가락/도구 개수 정상인가
- 날이 소재 표면에 실제로 닿는가 (붕 떠서 자르는 것처럼 안 보이는가)
- 소재 물성이 씬 사이에 안 바뀌는가 (색/질감 튐 없는가)
- 롱폼 추가: 클립 끝 프레임이 다음 클립 첫 프레임과 밝기 차 심하지 않은가
- 통과 못하면 재생성. 통과분만 최종 합본.

## 5단계 — 롱폼 조립 (롱폼일 때만)

클립 → 마스터 → 1시간 루프. 별도 편집 툴 없이 ffmpeg로 끝낸다.

**구조:** 유니크 클립 40개(각 8초) = 5분 마스터 → 12회 반복 = 1시간.
크레딧이 모자라면 클립 20개 / 3분 마스터 / 20회 반복으로 줄인다.

```bash
# list.txt: 클립마다 한 줄씩  file 'clip01.mp4'
ffmpeg -f concat -safe 0 -i list.txt -c copy master.mp4

# 라우드니스 균일화 + 루프 이음새 클릭음 제거용 앞뒤 페이드
# (st=299.9 는 마스터 길이 - 0.1초. 마스터 길이에 맞춰 고칠 것)
ffmpeg -i master.mp4 \
  -af "loudnorm=I=-20:TP=-2:LRA=7,afade=in:st=0:d=0.1,afade=out:st=299.9:d=0.1" \
  -c:v copy master_norm.mp4

# 1시간으로 루프
ffmpeg -stream_loop -1 -i master_norm.mp4 -t 3600 -c copy 1hour.mp4
```

주의:

- `-c copy` 는 클립 코덱/해상도/fps가 전부 같아야 동작한다. 다르면 먼저 통일 인코딩.
- 앞뒤 페이드 안 넣으면 루프마다 툭 소리 난다. 수면용에서 이건 치명적.
- YouTube 반복 콘텐츠 정책 리스크가 있으니 마스터는 5분 이상으로 가고,
  클립 순서를 섞은 마스터를 2~3종 만들어 번갈아 붙인다.
- 개별 클립 파일은 전부 보관한다. 나중에 숏폼으로 재활용한다.

## 6단계 — 라벨링 (건너뛰면 손해)

합성 콘텐츠 라벨 필수. YouTube "변경되었거나 합성된 콘텐츠" 토글 / TikTok AI 라벨.
라벨 누락 적발 시 도달률 페널티 + 삭제 사례 있음. 무조건 켠다.

## 7단계 — 배포 메타데이터 + 다국어 키워드

제목/태그의 뼈대는 0단계에서 고정한 니치 키워드로 통일한다 (매번 다른 소재로 흩어지지 않게).

ASMR·수면 영상은 언어 장벽이 없어서 해외 유입이 국내 유입보다 크다.
그래서 **소재 핵심 명사를 주요 언어로 병기**한다.

### 규칙

1. 소재에서 핵심 명사 1~2개만 뽑는다 (예: `용암`, `자르기`).
2. 아래 언어 목록으로 병기한다. 표기가 겹치면 한 번만 쓴다 (lava 중복 제거).
3. 문장을 번역하지 않는다. 명사만. 문장 다국어 나열은 스팸 판정 위험.
4. 제목은 영어 하나로 간다. 다국어는 설명 하단 `Keywords:` 줄과 태그 필드에만.
5. 태그 필드는 총 500자 제한. 영어 니치 키워드를 먼저 넣고 남는 자리에 다국어를 채운다.
6. 한자·가나·데바나가리·아랍·태국 문자는 그 나라 검색 유입에 직접 걸리니 빼지 않는다.

### 언어 목록 (예: 한국어 `용암`)

| 언어 | 표기 |
|---|---|
| English | lava |
| 中文 | 熔岩 |
| 日本語 | 溶岩 |
| Español | lava |
| Português | lava |
| Bahasa Indonesia | lava |
| हिन्दी | लावा |
| العربية | حمم بركانية |
| Русский | лава |
| Tiếng Việt | dung nham |
| ไทย | ลาวา |
| Deutsch | Lava |
| Français | lave |
| Türkçe | lav |
| 한국어 | 용암 |

### 출력 예시

```
Keywords: lava, 熔岩, 溶岩, लावा, حمم بركانية, лава, dung nham, ลาวา, lave, lav, 용암,
lava cutting asmr, glass cutting asmr, sleep asmr, 1 hour asmr, no talking
```

목표 지표: 숏폼 완주율 TikTok 70%+ / YouTube Shorts 75%+. 롱폼은 평균 시청 지속시간 기준으로 보고,
초반 30초 이탈이 크면 첫 클립부터 교체한다.

## 8단계 — 크레딧 가드레일

Veo 3.1 Fast 기준 (Google AI Pro, 월 1,000 크레딧):

| 항목 | 소모 |
|---|---|
| 8초 클립 1개 (Fast, 오디오 포함) | 20 크레딧 |
| 월 생성 가능 | 약 50클립 |
| 롱폼 1편 (유니크 40클립) | 800 크레딧 |
| 검수 탈락 재생성 30~50% 감안 | 실질 월 1편이 한계 |

크레딧이 모자라면 이 순서로 줄인다: 씬 재시도 횟수 → 클립 개수 → 마스터 길이.
마스터를 짧게 하고 반복 횟수를 늘리는 쪽이 제일 싸지만, 3분 미만은 루프 티가 나니 하한선으로 둔다.

월 2편 이상이 필요해지면 구독 상위 티어보다 Gemini API 종량제(약 $0.15/초, 8초당 $1.2)가 싸다.

크레딧은 이월되지 않고 결제 주기 끝에 소멸한다. 월말에 남으면 다음 편 클립을 미리 뽑아둔다.
