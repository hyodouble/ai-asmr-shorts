---
name: ai-asmr-shorts
description: 상품/쿠팡 없이 AI로 ASMR 영상을 만든다. 수면용 1시간 롱폼(10초 클립 루프)과 숏폼 둘 다 대응하고, 기획·씬 프롬프트·렌더 검수·롱폼 조립·배포 메타데이터까지 생성한다.
---

# AI ASMR — 수면용 롱폼

Google Flow의 **Omni 1.1 Flash**로 10초 클립을 여러 개 뽑아, 이어붙이고 반복해 1시간 수면용 영상을 만든다.
사람이 보다가 잠드는 게 목적이다. 그래서 규칙 하나가 다른 모든 규칙을 이긴다: **자던 사람을 깨우지 않는다.**

## 니치 — 스퀴시 ASMR (2026-09-01 확정)

두 갈래를 같은 채널에서 섞는다. 둘 다 "부드러운 물체를 천천히 누르면 잔 소리가 촘촘히 난다"는 한 가지 청각 경험이라 톤이 안 튄다.

1. **타바 스퀴시(taba squishy)** — 투명 필름에 말랑한 반죽을 봉하고 공기 방울을 주입한 장난감.
   누르면 방울이 밀려 다니며 뽀글거리고 작은 방울이 톡톡 터진다. TikTok·YouTube에 이미 1시간 수면 버전이 있을 만큼 검증된 포맷.
2. **왁뿌볼** — 말랑이 겉에 얇은 왁스 껍질을 굳힌 국내 유행 완구. 누르면 껍질이 잘게 깨지며 와그작거린다.

두 소재의 공통점이 채널 정체성이다: **잔 소리가 촘촘히 이어진다.** 큰 소리 하나로 터뜨리는 연출은 이 채널에 없다.

## 도구·계정

- 생성: Google Flow — https://labs.google/fx/tools/flow
- 계정 2개. **매번 로그인 계정과 크레딧 잔량을 먼저 확인한다** (우상단 아바타 클릭)
  - **본계정**(Chrome 첫 프로필) — **매일 50 크레딧 리셋**. Omni 1.1 Flash 나온다(2026-09-11 확인). 숏폼 3클립(45)이 딱 들어간다
  - **부계정**(Google AI Pro 월 1,000) — 롱폼·재생성용. 멤버십 만료일 확인(2026-09-22)
  - 계정 주소는 이 문서에 적지 않는다(공개 레포). Claude 메모리 `youtube-upload-via-chrome` 참고
- 모델: **Omni 1.1 Flash**. 720p / 10초 / x1 = **15크레딧**. Veo 3.1은 쓰지 않는다(같은 프롬프트에서 재질을 제멋대로 바꾼다)
- Flow UI가 자주 먹통이다. 프롬프트를 넣은 뒤 **스크린샷으로 텍스트가 박혔는지 확인하고 전송**할 것. 로딩 중 클릭은 씹힌다
- 후처리: ffmpeg, python(numpy). 스킬 폴더의 `detone.py` / `tonecheck.py`

## 1단계 — 프롬프트는 사용자 승인 후 입력한다

크레딧이 곧 돈이다. 씬 목록과 프롬프트 전문을 먼저 보여주고, 승인받은 뒤에 Flow에 넣는다.

## 2단계 — 프롬프트 구성

한 문단으로 쓴다. **A·D·E는 모든 클립에서 글자 그대로 고정**, B·C만 바꾼다. 고정 블록이 흔들리면 클립마다 톤이 튀어 루프에서 티가 난다.

**A. 카메라 (고정)**
```
Extreme close-up, macro lens, static locked-off camera, single continuous take, slow motion.
```

**B. 피사체 (변주)** — 재질을 반드시 명시한다. 안 쓰면 모델이 제 사전지식대로 다른 물건을 만든다.
```
Two human fingers slowly press a <색> <형태> <소재 설명> resting on <바닥>, <배경>.
```

**C. 동작 (변주하되 규칙 고정)** — 손이 **10초 내내 멈추지 않는다**고 반드시 쓴다. 안 쓰면 중간에 손이 멎고 무음 구간이 생긴다.
```
The fingers never stop moving for the entire clip: they press, roll and knead without pause,
so <잔 소리가 나는 현상> continues from the first frame to the last.
```

**D. 오디오 (고정)** — ASMR은 소리가 시청 이유의 70%다. 반드시 별도 문장으로.
```
Audio is the most important part of this video: <소재별 잔 소리>, hundreds of tiny overlapping
sounds at the same steady level from the first frame to the last, with no silent gaps.
Recorded very close, like an ASMR microphone right next to the object, soft and detailed.
Everything stays evenly loud the whole time - no single loud pop, no sudden burst, no impact,
no ringing, no bell, no chime. No music, no voice.
```

**E. 조명·금지 (고정)**
```
Soft diffused light from the upper left, even and steady.
No text, no on-screen captions, no faces, no speech, no jump cuts, no camera movement,
no morphing, no vanishing objects.
(커팅 포맷이면 여기에 칼 보호 문장을 덧붙인다 - 아래 "숏폼 30초" 절)
```

### 소리 원칙 (1번 규칙)

시청자는 자려고 트는 사람이다. 조용해서 볼륨을 올려둔 채 잠들었는데 피크가 한 번 터지면 그대로 깬다. 한 번 깨우면 그 채널은 다시 안 튼다.

- 좋은 소리 = **잔 소리가 촘촘히, 같은 크기로 이어지는 것**. 방울 뽀글거림, 왁스 사각거림, 반죽 눌리는 소리
- 나쁜 소리 = **레벨 점프**. 큰 파열음 하나, 종소리, 금속 울림, 정적 뒤의 갑작스런 소리
- 방울 터지는 소리는 넣는다. 단 `many tiny bubbles popping softly and continuously`처럼 **작고 촘촘하게** 지정한다
- **손바닥으로 내려누르는 동작**은 그림에 변화를 주니 몇 클립 섞는다. 단 타격이 아니라 압착으로 쓴다:
  `a slow cushioned press into the soft dough, not a hard hit` + `a broad soft muffled squelch of dough spreading, low and rounded, never a slap or a clap or a thud`
  이 문구 없이 "내려친다"만 쓰면 찰싹 소리가 붙어서 수면용으로 못 쓴다

## 3단계 — 씬 변주 (1시간 지루함 방지)

같은 그림이 반복되면 금방 이탈한다. 20~24클립을 아래 4축에서 조합이 안 겹치게 뽑는다.

| 축 | 값 |
|---|---|
| 소재 | 타바 스퀴시(방울) · 왁뿌볼(왁스 껍질) · 필름 속 반죽 · 젤리 코어 |
| 색 | 라벤더 · 민트 · 살구 · 크림 · 연회색 · 연분홍 |
| 바닥 | 린넨 천 · 대리석 · 자작나무 · 코르크 · 종이 · 벨벳 |
| 동작 | 두 손가락 누르기 · 손바닥 굴리기 · 손톱으로 눌러 밀기 · 방울 몰기 · 천천히 접기 · **손바닥으로 내려누르기** |

**바닥이 소리를 바꾼다.** 딱딱한 나무·대리석에 조각이 떨어지면 울린다. 조각이 떨어지는 소재는 **천 위에 놓는다.**

## 4단계 — 렌더 검수 (승인 전 필수)

- 손가락 개수 정상인가. 손이 프레임 안에 있는가
- 커팅 포맷이면 **칼날에 금·이빠짐이 없는가** (모델이 칼에도 크랙을 그린다)
- 소재가 중간에 다른 물건으로 안 바뀌는가 (색·질감 유지)
- 손이 10초 내내 움직이는가. 무음 구간 없는가
- 마지막 2초에 물체가 사라지거나 다시 붙지 않는가
- 다음 클립 첫 프레임과 밝기 차가 심하지 않은가 (경계 YAVG 차 8 이하)

### 오디오 검수 — 귀로만 하지 말 것

**모델은 프롬프트 금지어를 무시하고 벨/차임 효과음을 넣는다.** `no chimes, no bells, no ringing`을 넣어도 3회 연속 들어갔다.
프롬프트를 손질해 재생성하지 말 것 — 크레딧만 나간다. **후처리로 지운다.**

```bash
ffmpeg -i clip.mp4 -ac 1 -ar 48000 -c:a pcm_s16le clip.wav -y
python tonecheck.py clip.wav        # 톤 프레임 수 / 최대 prominence
python detone.py clip.wav clean.wav # 톤 성분 제거
```

- 벨은 스펙트로그램에서 **좁고 긴 가로줄**(한 주파수가 0.1~0.6초 지속). 왁스 크랙·방울 소리는 광대역이라 줄이 안 생긴다
- 실측 제거량: 2109 Hz -36 dB, 6199 Hz -22 dB, 4805 Hz -25 dB. 광대역 RMS 변화 0.3 dB라 잔 소리는 안 깎인다
- 빈 3개 이상 넓게 솟은 피크는 안 건드린다. 벨이 아니라 진짜 파열음이다
- **`detone.py`는 만능이 아니다.** 적용 전후로 `tonecheck.py`를 다시 돌려 비교할 것.
  실측 사례(2026-09-13): 경계값 톤 1프레임/34.5 dB짜리 클립에 걸었더니 22프레임/53 dB로 **악화**했다. 악화되면 원본을 쓴다
- **라우드니스는 loudnorm 이후에 잰다.** 원본 LRA 12~21도 처리 후 2~6으로 내려온다. 처리 후 **LRA > 10 탈락**

## 5단계 — 후처리 (클립마다)

```bash
ffmpeg -i in.mp4 -i clean.wav -map 0:v -map 1:a \
  -vf "delogo=x=1130:y=570:w=60:h=56,scale=1920:1080:flags=lanczos" \
  -af "highpass=f=120:p=2,lowpass=f=11000:p=2,afftdn=nr=18:nf=-45,acompressor=threshold=-45dB:ratio=12:attack=5:release=500,loudnorm=I=-16:TP=-2:LRA=5,volume=<부족분>dB,alimiter=limit=0.89:level=disabled,afade=t=in:st=0:d=0.04,afade=t=out:st=9.96:d=0.04" \
  -c:v libx264 -crf 16 -preset medium -c:a aac -b:a 192k out.mp4
```

- **워터마크**: 한국 계정은 Flow 가시 워터마크가 강제다(토글 잠김). 720p 좌표가 위 `delogo` 값. 1080p 업스케일본은 `x=1695:y=855:w=90:h=84`
- **컴프레서는 loudnorm 앞에.** 원본이 -37~-48 LUFS로 작고 들쭉날쭉해 뒤에 걸면 못 잡는다
- **`alimiter`는 기본값이 `level=true`라 리밋 후 출력을 도로 올린다.** `limit=0.5`를 걸고도 피크가 +0.6 dBFS로 클리핑났다. 반드시 `level=disabled`
- loudnorm 단독으로는 -22 LUFS까지밖에 안 올라온다. 측정 → `volume` 보정 → 리미터 순서
- **소음 제거는 증폭 전에.** 원본이 -37~-48 LUFS라 20 dB씩 올려야 하는데, 그 전에 안 자르면 잡음도 같이 올라와서 "소음이 심하다"가 된다.
  실측 대역 분포: 신호는 **1~10 kHz**에만 있다. 20~200 Hz는 상시 럼블(조용한 구간과 큰 구간 차이가 8.7 dB뿐), 10 kHz 위는 히스(차이 11.2 dB).
  `highpass=f=120` + `lowpass=f=11000` + `afftdn=nr=18:nf=-45` 로 SNR 29 → 39 dB. 신호 피크는 그대로다
- **라우드니스 목표는 -20 ~ -24 LUFS.** 원본이 작아 -16까지 밀면 리미터가 계속 물려 소리가 뭉갠다.
  수면용은 시청자가 볼륨을 직접 맞추니 조용한 쪽이 낫다. 크게 만들려다 노이즈를 같이 키우지 말 것
- **이음새는 클립마다 40ms 페이드.** 없으면 경계에서 -37~-43 dBFS 샘플 점프(틱 소리), 페이드 후 -120 dBFS
- 개별 클립 원본은 전부 보관한다. 숏폼으로 재활용한다

### 왁스 클립은 컴프레서 2단 (2026-09-02 추가)

왁스 크랙은 임펄스라 1단(-45/12)으로는 LRA가 14~20까지 뜬다. 왁스 소재만 앞단을 하나 더 건다:

```
acompressor=threshold=-50dB:ratio=20:attack=3:release=800,acompressor=threshold=-30dB:ratio=8:attack=3:release=400
```

실측: 같은 클립 LRA 14.0 → 8.4. 타바(방울)는 1단으로 충분하다.

### 무음 리드인 차단 문구 (2026-09-02 추가)

"손이 다가와서 누르기 시작"하면 앞 1~2초가 -65 dB로 비어 LRA가 터진다. B·C·D에 아래를 박는다:
`Two human fingers are already pressing ... in the very first frame` / `already at full level in the very first frame` / `no silent gaps and no quiet lead-in`.
그래도 소리가 안 실리는 클립이 나온다(20개 중 1개는 원본 -50 LUFS로 사실상 무음) — 후처리로 못 살리니 버린다.

### 배치 처리 주의

- ffmpeg는 stdin을 먹는다. 스크립트 루프에서는 **`-nostdin` 필수**, 안 붙이면 첫 클립 뒤 루프가 죽는다
- Flow 다운로드는 Chrome이 연속 다운로드를 막는다. 주소창 차단 아이콘에서 labs.google 자동 다운로드를 허용해야 한다

## 6단계 — 1시간 조립

20클립 × 10초 = 200초 마스터. 순서를 섞은 마스터 2종(A·B)을 번갈아 붙여 6분 40초 사이클 → 9회 반복 = 1시간.

```bash
ffmpeg -f concat -safe 0 -i listA.txt -c copy masterA.mp4
ffmpeg -f concat -safe 0 -i listB.txt -c copy masterB.mp4
ffmpeg -f concat -safe 0 -i cycle.txt -c copy cycle.mp4
ffmpeg -i cycle.mp4 -af "loudnorm=I=-16:TP=-2:LRA=5" -c:v copy cycle_norm.mp4
ffmpeg -stream_loop -1 -i cycle_norm.mp4 -t 3600 -c copy 1hour.mp4
```

- `-c copy`는 클립 코덱/해상도/fps가 전부 같아야 동작한다
- YouTube 반복 콘텐츠 정책 리스크가 있으니 사이클은 5분 이상으로 간다
- 최종본도 한 번 더 loudnorm을 통과시킨다. 개별 클립이 통과해도 이어붙이면 편차가 생긴다

## 7단계 — 업로드

- **합성 콘텐츠 라벨 필수.** YouTube "변경되었거나 합성된 콘텐츠" 토글. 누락 적발 시 도달률 페널티·삭제 사례 있음
- 제목은 영어 하나로. **번역은 기본으로 하지 않는다** — 아래 "배포 규칙" 절 참고
- 원본(영어) 설명은 반드시 채운다

## 8단계 — 크레딧

| 항목 | 소모 |
|---|---|
| 10초 클립 1개 (720p, x1) | 15 |
| 20클립 1편 | 300 |
| 검수 탈락 재생성 30% 감안 | 약 390 |

Google AI Pro 월 1,000 크레딧. 이월 없이 결제 주기 끝에 소멸하니 월말에 남으면 다음 편 클립을 미리 뽑는다.
멤버십 만료일 확인(2026-09-22).

## 9단계 — 가로 소스를 쇼츠로 구제 (2026-09-12 발견)

Flow에서 9:16으로 바로 뽑으면 해당 없음. 옛 롱폼 클립 등 **가로(16:9) 소스만 있을 때**만 쓴다.

- **가로 그대로 올리면 유튜브가 롱폼 "동영상"으로 분류해 Shorts 탭에 안 걸린다.** 반드시 9:16으로 재렌더링 후 업로드.
  업로드 후 `youtube.com/shorts/...` 링크가 뜨는지로 확인한다(가로면 `youtu.be/...`만 뜬다)
- **풀블리드 센터크롭**이 블러 패딩보다 완주율이 좋다. 단 피사체가 중앙에 있는 매크로 구도에서만 안전하고,
  양손 쓰는 씬은 반대쪽 손끝이 잘린다 — 그런 구도만 블러 패딩으로 예외 처리
- 크롭 예시(1280x720 → 1080x1920): `crop=405:720:437:0,scale=1080:1920:flags=lanczos`
- **크롭·딜로고 단계에서 `-an`을 쓰지 말 것.** 원본 클립에 Flow가 넣은 오디오가 있는데 무심코 `-an`을 넣어
  무음 ASMR을 업로드할 뻔했다(2026-09-13). 오디오는 `-map 0:v -map 1:a`로 반드시 같이 옮긴다

---

## 숏폼 30초 (10초 x 3) — 2026-09-11 추가

> **이 절의 길이·클립 수는 2026-09-22 "숏폼 포맷 v2"로 대체되었다.** 30초 3클립과 15~18초 압축은 더 이상 쓰지 않는다.
> 트렌드 조사·Flow 조작·후처리 체인·업로드 규칙은 이 절 그대로 유효하다. 충돌하면 v2가 이긴다.

수면용 롱폼과 **규칙이 다르다.** 숏폼 시청자는 자는 사람이 아니라 스크롤하는 사람이다.

### 하루 루틴 (이 순서 그대로)

1. **트렌드 조사 — 영상 만들 때마다 먼저 한다.** 지금 유튜브에서 hype가 가장 높은 AI 영상을 찾아 그 포맷을 만든다. 니치는 고정하지 않는다. 방법은 아래 "트렌드 조사" 절
2. **분석 → 소재 결정** — 1등 영상을 분석 항목대로 뜯고, 같은 포맷 안에서 기존 영상과 안 겹치는 소재를 고른다(아래 "발행 이력" 표를 본다)
3. **프롬프트 3개 작성 → 사용자 승인** (1단계 규칙은 숏폼에도 그대로 적용). 참고 템플릿: `templates/short-glass-cutting.txt` — 유리 커팅 예시다. 포맷이 바뀌면 문장 구조(A~E 고정 + 3막)만 빌려 쓰고 내용은 새로 쓴다
4. **Flow 설정 확인** — 로그인 계정, 크레딧 잔량, 그리고 에이전트 설정에서 **9:16 / x1 / Omni 1.1 Flash**
5. **클립 3개 생성** — 한 번에 하나씩, 승인 카드에서 승인. 45크레딧
6. **다운로드** — 클립 편집 화면 우상단 아이콘 → 720p 원본 크기
7. **검수** — 소재 유지 / 카메라 고정 / 손·칼이 계속 움직이는지 / 무음 구간 없는지.
   `ffmpeg -i s1.mp4 -vf "fps=1,scale=270:-1,tile=5x2" -frames:v 1 grid.png` 로 10프레임을 한 장에 뽑아 본다
8. **조립** — `raw/s1.mp4 s2.mp4 s3.mp4` 로 이름 바꿔 넣고 `sh scripts/build-short.sh <작업폴더>`
9. **업로드** — 7단계 + 아래 배포 규칙 (영어 원본 설명만 채운다)

작업 폴더는 `Desktop/asmr_work/shorts/<YYYYMMDD>/` 로 판다.

- 니치: **고정하지 않는다.** 조사에서 1등으로 나온 포맷을 그대로 만든다(유리 커팅·용암 절단·얼음 조각·자석 슬라임 등 무엇이든). 글래스 커팅은 2026-09 시점의 1등이었을 뿐이다
- 3막으로 연속성을 만든다: 셋업 → 절정 → 해소. 유리 커팅이면 실금 → 관통·파편 → 단면 공개. A·B·D·E 고정, C만 바꾸는 원칙은 롱폼과 같다
- 금지어·후처리는 **그 포맷의 소리에 맞춰 켜고 끈다.** 유리·얼음처럼 쨍한 소리가 상품이면 `no ringing, no bell, no chime` 를 빼고 `detone.py` 도 돌리지 않는다. 말랑한 소재(슬라임·반죽·왁스)면 롱폼 규칙대로 금지어를 넣고 `detone.py` 를 돌린다
- **칼은 절대 금이 가면 안 된다.** 모델이 칼날에도 실금을 그려 넣는다(2026-09-17 유리 디저트·레진 큐브에서 확인).
  프롬프트 E 블록 끝에 이 문장을 **항상** 넣는다:
  `The steel blade itself stays perfectly solid and undamaged for the entire clip - it never cracks, chips, bends or shatters, and no fractures appear on the blade; only the object being cut breaks.`
  검수 그리드(4단계)에서도 **칼날에 금이 갔는지 따로 본다.** 칼이 깨지면 그 클립은 탈락이다
- Flow 설정: 동영상 기본값 **9:16 / x1 / Omni 1.1 Flash**. 3클립 = 45크레딧 = 본계정 하루 한도(50) 안

### 트렌드 조사

브라우저 도구로 유튜브를 직접 본다. 기억이나 추측으로 포맷을 정하지 않는다.

- **어디를 보나** — 유튜브 검색 `AI ASMR`, `#aiasmr`, `veo asmr`, `ai video shorts` 를 필터 **업로드 날짜: 이번 주 + 정렬: 조회수** 로. Shorts 피드도 같이 훑는다
- **hype 판정은 조회수가 아니라 속도다** — 조회수 ÷ 업로드 후 경과일. 7일 안에 100만을 넘긴 게 있으면 그게 이번 주 1등이다. 같은 포맷이 서로 다른 채널에서 여러 개 올라와 있으면 확산 중이라는 뜻이라 우선한다
- **늦은 포맷은 버린다** — 상위 영상이 전부 2주 이상 전이면 이미 끝물이다. 다음 후보로 넘어간다

분석 항목(1등 영상에서 이 6개를 받아적는다):

1. 무엇을 무엇으로 바꿨나 (치환 규칙. 예: 과일 → 유리)
2. 페이오프 동작이 뭔가 (자르기·누르기·터뜨리기·녹이기)
3. 소리 정체가 뭔가 (쨍 / 바스락 / 뽀글) — 금지어와 후처리 분기가 여기서 갈린다
4. 카메라·바닥·조명
5. 3막이 있나. 첫 1초에 뭐가 보이나 (스크롤 정지 요인)
6. 댓글에서 사람들이 뭘 좋아한다고 쓰나

조사 결과는 작업 폴더에 `trend.md` 로 남긴다. 다음 영상에서 같은 포맷을 이어갈지 판단하는 근거가 된다.

### 새 Flow UI (에이전트 방식)

- 좌측 `장면` → 우측 에이전트 패널에 프롬프트를 넣는다. 설정은 프롬프트 상자 옆 톱니(에이전트 설정)
- 프롬프트 앞에 `Generate exactly one 10-second video using the following prompt verbatim. Do not rewrite, shorten, translate or add to it. PROMPT:` 를 붙인다. 안 붙이면 에이전트가 프롬프트를 제 맘대로 고쳐 쓴다
- **에이전트가 "I'm going to generate..." 만 하고 멈추는 일이 잦다.** 승인 카드가 안 뜨면 `Go ahead and generate that video now.` 한 줄을 더 보낸다. 그러면 승인 카드가 뜬다
- 다운로드는 클립 편집 화면 우상단 아이콘 → **720p 원본 크기**

### 후처리 (글로벌 1위 쇼츠 표준 클린 체인 적용)

- **과도한 필터(`afftdn`, `dynaudnorm`, 10:1 `acompressor`) 전면 폐기**:
  - 인위적인 FFT 노이즈 리덕션(`afftdn`)은 미세한 빗소리, 바람소리, 물결 소리를 로봇 쇳소리나 물속 소리로 심각하게 왜곡(Phasing Artifact)시킨다. 글로벌 인기 쇼츠 채널처럼 원음의 미세 결(Air & Transient)을 그대로 보존한다.
  - `dynaudnorm`은 숨소리 펌핑(배경 노이즈가 울렁거리며 커졌다 작아지는 현상)을 유발하므로 사용하지 않는다.
  - 10:1 비율의 과도한 라디오 컴프레서는 미세 다이내믹스를 파괴해 거친 백색소음처럼 뭉개므로 폐기한다.
- **표준 클린 체인**:
  - `highpass=f=45:p=2` (45Hz 이하의 초저주파 럼블만 완만히 컷, 60~120Hz의 따뜻한 저음 공명 보존)
  - 쇼츠 피드 재생 표준 라우드니스 **`-14.0 ~ -15.0 LUFS`** 타겟 (`loudnorm=I=-14:TP=-1.0:LRA=7` 또는 선형 게인 + True Peak 리미터)
  - 수면용 롱폼의 구 규격(-20 LUFS)을 쇼츠에 쓰면 다른 숏폼 대비 6 dB나 작아 시청자 스와이프 이탈이 발생하므로 금지한다.
- **True Peak Ceiling -1.0 dBTP 유지**:
  - 모바일 스트리밍 AAC 인코딩 오버슈트로 인한 디지털 클리핑을 막기 위해 리미터 천장을 항상 `-1.0 dBTP`로 유지한다.
- 워터마크 delogo 좌표(720x1280 원본): `x=574:y=1138:w=56:h=56`.
- 최종 규격: 1080x1920 / 24fps Lanczos / **-14.0 ~ -15.0 LUFS** / 클립 간 편차 1.0 dB 이내 / True Peak < -1.0 dBTP

- **브라우저 업로드 도구는 한 번에 10MB까지만 보낸다.** 1080x1920 30초 CRF 18 원본이 18MB라 그대로는 못 올린다.
  빌드 스크립트가 `out/upload.mp4`(CRF 23, 약 9.5MB)를 같이 만들어 둔다. 원본이 720p 업스케일이라 눈에 띄는 손해는 없다.
  원본 화질 그대로 올리려면 사람이 Studio에서 직접 파일을 고르면 된다

파이프라인 스크립트(2026-09-21 개편, 순서대로 돌린다). 작업 폴더에 `raw/s1..s3.mp4` 와 `foley/` 를 넣고:

```bash
sh scripts/mix-foley.sh <작업폴더>                       # foley 레이어 (BED, FOLEY_DB 로 조절)
GAIN1=0 GAIN2=0 GAIN3=0 sh scripts/build-short2.sh <작업폴더>   # -14.5 LUFS, 편차 1 dB 이내로 GAIN 실측 보정
python3 scripts/make_overlays.py <작업폴더> "<후크>" "1. ..." "2. ..." "3. ..."
LIMIT=0.7 GFIN=0 sh scripts/finish-short.sh <작업폴더>   # 오버레이 + 1.7배속, 최종 -14.0~-15.0 LUFS로 GFIN 보정
```

구 `scripts/build-short.sh`(1패스 loudnorm)는 조용한 원본에서 목표 라우드니스에 못 닿아 쓰지 않는다.
클립 간 라우드니스가 1.5 dB 넘게 벌어지면 `GAIN1/GAIN2/GAIN3` 환경변수(dB)로 맞춰 다시 돌린다.
프롬프트 템플릿: `templates/short-glass-cutting.txt` (2026-09-11 유리 키위 3막 전문)

### 배포 규칙 — 번역은 뜬 영상에만 (2026-09-16 변경)

**기본은 영어 원본 설명 하나다.** 업로드할 때 번역은 달지 않는다.

- 원본(영어) 설명은 반드시 채운다. 안 채우면 영어권 시청자에게 설명이 안 보인다 (2026-09-06 쇼츠 4개가 이 상태로 올라가 있었고 9/11에 채워 넣었다)
- **조회수 10만을 넘긴 영상에만** 번역을 소급해서 단다. 그때 언어는 **한국어 · 일본어 · 스페인어 · 포르투갈어**
- 방법: Studio 좌측 **자막** → `언어 추가` → 언어별 **제목 및 설명** 번역을 게시한다. 자막 파일(SRT)은 안 올린다 — 말이 없는 영상이라 필요 없다

**왜 바꿨나** (2026-09-16, 5개 언어를 두 편에 달아 본 뒤):

- 숏폼 유통은 검색이 아니라 피드 추천이고, 추천은 완주율·재시청 같은 시청 신호로 돈다. 번역이 효과를 내는 검색·탐색은 숏폼에서 비중이 작다
- 말이 없는 영상이라 언어 장벽 자체가 없다. 번역이 풀어줄 마찰이 없다
- 유튜브가 제목·설명을 자동 번역해 보여주기도 해서 수동 번역분이 묻힌다
- 비용이 크다. 영상 1편당 자막 4개 = 브라우저 조작 40~50스텝으로, 제작 전체 시간의 절반 가까이를 먹었다
- 채널 규모(구독 12, 최고 1.3천 조회)에서는 차이를 측정할 수도 없다. **뜬 영상에만 손대면 같은 효과를 1/10 비용으로 얻는다**

설명 형식은 5문단 고정:
```
[영상에서 무슨 일이 일어나는지 1문단]

No narration, no music - [이 영상에서 들리는 소리].

Made with AI video tools. The audio is generated, then cleaned up and level-matched by hand.

Best with headphones.

#asmr #[소재] #shorts
```

### 알고리즘 돌파 및 채널 최적화 규칙 (2026-09-19 추가, 2026-09-20 정립)

#### 0. 조회수 정체(1.1K~1.2K Seed Test 한계) 원인 분석
- **YouTube Shorts 알고리즘 작동 원리**: 신규 영상은 약 1,000~1,200명의 초기 시청자(Seed Pool)에게 테스트 배포된다. 이 단계에서 **VVSA(조회율)**와 **APV(평균 시청 지속 시간)**가 기준치를 넘지 못하면 알고리즘이 추천 피드 노출을 즉시 중단한다.
- **주요 저해 원인**:
  1. **30초 고정 길이로 인한 낮은 완주율(APV)**: 30초 영상에서 시청자가 10~15초만 보고 넘기면 완주율이 33~50%에 불과하여 알고리즘이 "매력 없는 콘텐츠"로 판정.
  2. **첫 1초 시각적 후크 부재로 인한 스와이프 이탈(VVSA 저하)**: 화면 내 텍스트나 질문이 없으면 단순 AI 비디오로 인식되어 1초 내에 위로 넘김.
  3. **소재의 잦은 변경으로 인한 시청자층 미형성**: 유리, 스퀴시, 용암, 슬라이드 등 매일 포맷이 급변하여 충성 시청자 페르소나 및 알고리즘 타겟팅 실패.
  4. **언어 혼용(한글 노출)에 의한 글로벌 트래픽 왜곡**: 글로벌 ASMR 타겟 영상에 한글(설명/댓글 등)이 섞이면 다국어 알고리즘 추천 풀이 제한됨.

#### 1. 목표 지표 (YouTube Shorts 알고리즘 기준)
- **VVSA (Viewed vs Swiped Away)**: 75%~85% 이상 유지 (첫 1초 이탈 방지가 핵심)
- **APV (Average Percentage Viewed)**: 85%~120% 이상 달성 (100% 이상 시 무한 루프 효과로 바이럴)

#### 2. 채널 브랜딩 일치 (Bio 리뉴얼)
- **문제**: 채널 소개글이 "수면용 롱폼(to fall asleep to)"으로 되어 있으면, 쨍한 30초 숏폼 시청자가 구독을 누르지 않고 알고리즘도 타겟층 학습에 혼선을 겪는다.
- **규칙**: 채널 소개글을 숏폼 Satisfying ASMR 포맷에 맞춰 일치시킨다.
  > *"Satisfying AI ASMR & Oddly satisfying visual triggers. Which one would you choose? Wear headphones for the best experience."*

#### 3. 킬러 포맷 시리즈화 (시청자 페르소나 확립)
- 매번 트렌드 1등을 쫓아 소재를 급변(부시크래프트→스퀴시→유리→비누→슬라이드)하면 알고리즘이 충성 시청자층을 학습하지 못한다.
- 반응이 입증된 핵심 포맷(예: 글래스 커팅 또는 푸드 슬라이드)은 **최소 7~10편 이상 연작(#1, #2, #3...)**으로 밀어붙여 채널 시그니처를 각인시킨다 (대형 채널 DREAM DESIGN AI 방식).

#### 4. 영상 길이 최적화 (30초 고정 탈피) — **2026-09-22 v2로 대체**

> 15~18초 압축본 2편(19회 · 46회)으로는 배급이 안 돌아왔다. 길이 규칙은 v2(60~90초)를 따른다. 아래는 근거 보존용 기록이다.
- **30초는 숏폼에서 길다.** 시청자가 10~15초만 보고 넘기면 완주율이 33~50%로 떨어져 1.2K에서 알고리즘이 추천을 즉시 중단한다.
- **개선**: 10초 클립 3개를 1.3~1.5배속으로 편집하거나 핵심만 타이트하게 잘라 **전체 길이를 15~18초로 압축**한다.
- **무한 루프(Infinite Loop)**: 마지막 씬의 끝동작과 첫 씬의 시작동작을 매끄럽게 연결해 1회 완주 후 2회차 재생을 유도(APV 120%+ 달성).

#### 5. 첫 1초 후크(Hook) & 텍스트 오버레이 (VVSA 극대화)
- `No text` 규칙만 고수하면 피드에서 빠른 스크롤 중 단순 AI 영상으로 오인해 넘겨버린다.
- **규칙**: 영상 상단에 시각적 질문 텍스트와 씬 넘버링을 삽입한다.
  - 상단 후크: `Which slide are you picking? 🍓 vs 🥭 vs 🫐` (또는 `Which one would you choose? 👇`)
  - 각 씬: `1. Strawberry`, `2. Mango`, `3. Blueberry` 넘버링 표기.

#### 6. 사운드 레이어링 (실제 Foley 효과음 합성)
- Flow/Veo 모델이 생성한 원본 오디오는 노이즈가 많고 타격감/선명도가 떨어진다.
- AI 생성 오디오 위에 **실제 고음질 ASMR 효과음(실제 유리 깨지는 소리, 찰진 슬라이드 마찰음, 젤리 입수음 등)을 15~25% 볼륨으로 레이어링**하여 청각적 쾌감을 극대화한다.

#### 7. 인게이지먼트 유도 (첫 댓글 고정 & 투표)
- 영상 업로드 직후 채널 본인 계정으로 첫 댓글을 작성하고 **상단 고정(Pin)**한다.
  - 예: *"I personally love the Mango cream slide! 🥭 Which one are you riding? 👇"*
- 시청자의 댓글(1번, 2번 선택) 참여를 유도하여 알고리즘 인게이지먼트 점수를 확보한다.
- Shorts 투표 스티커(Poll Sticker)를 적극 활용한다.

#### 7-1. 캐릭터 포맷 예외 — 얼굴·목소리 허용 (2026-09-23 추가)

기본 E 블록의 `no faces` 와 D 블록의 `no voice` 는 **무생물 소재(유리·얼음·슬라임·스퀴시)에만 적용된다.**
프루트 베이비처럼 캐릭터가 주인공인 포맷에서는 그 두 금지어를 **뺀다.** 얼굴이 이 포맷의 스크롤 정지 요인이고,
아기 웃음소리가 청각 상품 자체라(1등 영상 제목에 `Baby laughing sound` 가 박혀 있다) 금지어를 그대로 두면 포맷이 성립하지 않는다.

- E 블록에서 제거: `no faces`
- D 블록에서 `No music, no voice.` → `No music, no spoken words, no narration.`
  **말은 여전히 금지한다.** 웃음·옹알이·먹는 소리만 허용한다. 대사가 들어가면 언어권이 갈려 글로벌 시드 풀이 좁아진다(규칙 8과 같은 이유)
- `no text` 는 **유지한다.** 모델이 그리는 글자는 깨진다. 후크 텍스트는 후처리로 얹는다(규칙 5)
- 나머지 고정 블록(정적 카메라·단일 테이크·조명·no jump cuts·no morphing)은 그대로 간다

#### 8. 언어 엄격 통일 (한글 완전 배제)
- 글로벌 ASMR 시청층 타겟이므로 제목, 설명문, 댓글 등 모든 메타데이터에 **한글을 일체 사용하지 않고 100% 영어**로만 작성한다. 한글이 섞이면 유튜브 알고리즘이 로컬 피드로 분류하여 초기 시드 풀 확장에 악영향을 준다.

#### 9. 글로벌 1위 쇼츠 유튜버 기준 오디오 마스터링 규칙 (2026-09-20 제정)

##### (1) 최근 소리가 이상하게 들렸던 원인 전수 분석 (레거시 수면용 규칙의 오류)
- **근본 원인**: 이전 규칙(-20.0 LUFS 및 과도한 필터 체인)은 **수면용 1~3시간 롱폼(16:9 가로 화면, 취침 전 저볼륨 감상)**을 위해 고안되었던 설정이었음. 이를 쇼츠에 무비판적으로 적용하면서 소리 품질이 심각하게 왜곡됨.
- **주요 왜곡 요인**:
  1. **`afftdn=nr=16:nf=-50` (인위적 FFT 노이즈 리덕션)**:
     - AI 영상 모델(Omni 1.1 Flash)이 생성한 바람 소리, 물결 소리, 빗소리, 울 직물 마찰음의 섬세한 고주파를 '노이즈'로 오인하여 깎아내면서, **물속에 들어간 듯한 먹먹함, 위상 왜곡(Phasing), 메탈릭한 로봇 사운드(Watery/Swirling Artifact)**를 유발함.
  2. **`dynaudnorm=f=150:g=15:m=100` (극단적 동적 볼륨 증폭)**:
     - 150프레임마다 게인을 최대 100배(`m=100`)까지 실시간 가변시킴으로써 조용한 순간과 소리가 나는 순간 사이에 노이즈 플로어가 오르락내리락하는 **'숨소리 펌핑(Breathing/Pumping) 현상'**을 유발함.
  3. **`acompressor` 10:1 라디오 방송용 압축 + 14dB 메이크업 (다이내믹스 파괴)**:
     - 10:1의 과도한 압축으로 ASMR의 생명인 미세 다이내믹스와 트랜지언트(물체에 닿는 순간의 쾌감 충격음)를 짓이겨, 소리가 평면적이고 거친 백색소음처럼 뭉개짐.
  4. **`highpass=f=90` (과도한 저음 컷)**:
     - 90Hz 이하를 급격히 잘라 매트리스를 누르는 묵직한 공명(60~80Hz)이나 바다의 깊은 울림이 사라져 소리가 깡통처럼 얇아짐(Thin sound).

##### (2) 가장 인기 있는 글로벌 쇼츠 유튜버들의 사운드 규칙 (업계 표준)
- **규칙 1: 쇼츠 피드 표준 라우드니스 `-14.0 ~ -15.0 LUFS` 타겟 (유튜브 공식 재생 표준)**
  - 유튜브 쇼츠 피드는 일반 숏폼과 동일한 재생 라우드니스 **-14.0 LUFS**로 자동 정규화된다.
  - `-20.0 LUFS`로 올리면 피드 내 다른 영상들보다 **6 dB나 작아서(소리 볼륨이 절반 수준)**, 폰 스피커로 넘겨보던 시청자가 소리를 전혀 인지하지 못하고 즉각 이탈한다.
  - 인기 쇼츠 채널은 **-14.0 LUFS ~ -15.0 LUFS** (True Peak -1.0 dBTP)를 타겟으로 하여, 시청자가 스마트폰 볼륨을 따로 키우지 않아도 귀에 꽉 찬 풍성한 만족감을 제공한다.
- **규칙 2: 글로벌 노이즈 리덕션(`afftdn`) 전면 금지 (Natural Texture 보존)**
  - 최고 인기 ASMR/쇼츠 사운드 엔지니어링의 황금률: *"The best noise reduction is none at all."*
  - Omni 1.1 Flash가 생성하는 네이티브 오디오는 이미 배경이 깨끗하다. 인위적인 FFT 노이즈 리덕션을 무차별 적용하지 않고 원음의 미세 결(Air & Transient)을 그대로 살린다.
- **규칙 3: 클린 신호 체인 (Clean Signal Chain)**
  - 불필요한 초저주파 럼블(진동)만 완만하게 컷: `highpass=f=45:p=2` (45Hz 이하만 컷, 60~120Hz의 따뜻한 저음 공명 보존)
  - `dynaudnorm`과 `10:1 acompressor`를 폐기하고, 업계 표준인 **`loudnorm=I=-14:TP=-1.0:LRA=7`** (또는 선형 게인 + True Peak -1.0 dBTP 리미터)를 적용하여 펌핑과 왜곡을 0%로 만든다.
- **규칙 4: 트랜지언트(첫 접촉음) 보존**:
  - 손이나 도구가 닿는 첫 10~20ms의 어택을 압축하지 않고 살려야 뇌에 소름이 돋는 ASMR 팅글(Tingles)이 형성된다.
- **규칙 5: True Peak Ceiling -1.0 dBTP 유지**:
  - 모바일 스트리밍 시 유튜브의 AAC/Opus 트랜스코딩 오버슈트로 인한 디지털 클리핑(찌직거림)을 완벽 차단하기 위해 마스터 True Peak는 항상 `-1.0 dBTP` 이하로 보호한다.




### Studio 조작 시 함정

- **페이지 로드 직후 첫 입력은 씹힌다.** 설명란을 클릭하고 타이핑했는데 비어 있으면 다시 클릭하고 다시 친다. 스크린샷으로 확인 후 다음 단계로
- **줄바꿈이 포함된 문자열을 한 번에 타이핑하면 전부 날아간다.** 한 문단씩 치고 사이에 Enter 두 번을 따로 보낸다
- 해시태그를 치면 자동완성 드롭다운이 뜬다. **Escape로 닫고** 저장한다
- 언어 목록은 검색이 안 된다. 목록에서 직접 찾아야 한다. 스크롤은 목록을 연 직후 첫 틱이 씹히니 한 번 더 굴린다
- **AI 사용(합성 콘텐츠)·유료 프로모션 질문은 기본으로 접혀 있다.** 세부정보 단계 "연령 제한" 아래 **"자세히 보기"**를 펼쳐야 나온다
- "다음" 버튼은 필수 항목 중 답 안 한 게 있으면 그 위치로 자동 스크롤해 준다 — 놓쳐도 복구된다
- 잘못 올린 영상 삭제: 체크박스 → 추가 작업 → 완전삭제 → "되돌릴 수 없음" 체크 → 완전삭제. 처리에 수 초~십여 초 걸린다
- 자막 번역 행은 "제목 및 설명" 칸을 한 번 클릭해 연필 아이콘을 띄운 뒤 다시 클릭해야 편집창이 열린다

### Flow 에이전트 함정 (2026-09-15 확인)

- **에이전트가 생성 중일 때 친 프롬프트는 통째로 씹힌다.** 전송 버튼이 정지(■)면 기다렸다가 친다. 친 뒤 스크린샷으로 확인할 것
- 프롬프트를 보냈는데 승인 카드도 응답도 안 뜨면 `Go ahead and generate that video now.` 한 줄을 더 보낸다(3클립 중 1클립 꼴로 발생)
- 다운로드 버튼은 zip으로 받아진다(`다운로드.zip`). 압축 안에 mp4 하나. 연속으로 누르면 `다운로드 (1).zip`, `(2).zip`로 쌓이니 순서를 기록해 둘 것

---

## 숏폼 포맷 v2 (2026-09-22) — 조회수 붕괴 진단 후 개정

### 무슨 일이 있었나 (SliceverseAI 실측)

| 구간 | 영상 | 조회수 | Shorts 피드 비중 |
|---|---|---|---|
| 9/11~9/17 (커팅) | 15편 | 446 ~ 1,277 | 94.3% |
| 9/18~9/21 (누르기·슬라이드) | 10편 | 10 ~ 145 | 28.0% |

- 최고 실적본(9/17 크리스털 침대, 1,237회): **계속 시청함 31.5%**, 평균 시청 0:15/0:31, 구독 전환 **+1명(0.08%)**, 댓글 0
- 9/19 슬라임 소파(145회): 계속 시청함 **48.4%** — 유지율은 오히려 더 높은데 조회수는 1/8. 즉 **영상이 나빠서가 아니라 피드가 안 밀어줘서** 줄었다
- 초기 1,000회대는 실력이 아니라 **신규 채널 탐색 물량**이었다. 그 물량이 소진된 뒤로는 유지율·참여로 배급을 새로 벌어야 하는데, 31~48% 유지 / 댓글 0 / 구독 0.08%로는 못 번다

### 경쟁 채널 실측 (같은 주)

| 채널 | 구독 | 편수 | 길이 | 최근 조회 |
|---|---|---|---|---|
| DREAM DESIGN AI | 1.1천 | 14 | 1~4분 | 14만 / 5.5만 / 4.4천 |
| Quiz Dino | 157만 | 754 | 25분 롱폼 + 클립 | 20만 |
| MidNight Ai Asmr | 소형 | 다수 | 30초~1분 | 22 ~ 171 (가끔 1.1만) |
| 유리·크리스털 커팅 신작 전반 | — | — | 30초~3분 | **1 ~ 20** |

판정 세 가지:
1. **유리·크리스털 커팅은 완전히 죽었다.** 이번 주 신작이 채널 불문 1~20회. 이 소재로는 다시 안 뜬다
2. **"Which ... Would You Choose" 선택형은 살아 있다.** 단, 이기는 쪽은 전부 **1~3분 길이**다
3. 1.1천 구독 채널이 14만을 내는 동안 우리가 40회인 차이는 **소재 발상**이다. 저쪽은 *불가능한 오브젝트*(콜라캔 침대, 키위 케이크 침대, 거대한 치아 침대)를 **광각 + 사람 스케일**로 보여준다. 우리는 천 위에 놓인 말랑한 공을 **매크로**로 눌렀다. 피드에서 0.5초 안에 "뭐야 저게"가 안 나온다

### v2 규칙 (다음 영상부터 이대로)

1. **길이 60~90초, 아이템 6~9개.** 30초 3클립을 버린다. 한 아이템 8~10초, 컷 사이를 같은 카메라·같은 바닥으로 이어 붙여 한 흐름으로 만든다
2. **광각 오브젝트, 매크로 금지.** 방·가구·사람이 화면에 같이 들어가는 크기. "이게 실제로 있을 리 없다"가 한 프레임에 보여야 한다. 손 + 작은 물체 + 천 배경 조합은 폐기
3. **화면 글자를 넣는다.** 상단에 질문 한 줄(`Which bed would you sleep in?`), 각 아이템에 번호 `1 2 3 ...`, 마지막 2초에 `Comment your pick 👇`. 지금까지 영상에는 글자가 하나도 없었고 댓글도 0이었다
4. **시리즈 연번으로 고정한다.** 제목 포맷 하나를 정해 `... #2`, `#3` 로 이어간다. 매일 새 주제로 갈아타지 않는다. 경쟁 채널은 같은 제목 14편으로 14만을 냈다
5. **하루 1편.** 9/21에 4편을 올려 17·21·38·47회였다. 편수로는 못 이긴다
6. **커팅 소재는 폐기.** 페이오프는 "선택 → 그 선택지를 눌러/잘라/터뜨려 보여주기" 중 하나로 유지하되, 유리·크리스털 과일은 쓰지 않는다
7. **씨앗 댓글은 발행 직후 채널 계정으로 단다.** 화면에 CTA를 띄워놓고 댓글창 첫 줄이 비어 있으면 아무도 시작하지 않는다.
   문구·계정 주의는 "7단계 — 업로드" 절 그대로. 고정(📌)은 **2026-10-05에 재확인 후** 규칙에 넣는다 — 그 전까지는 달기만 한다.
   **댓글이 없어서 안 뜬 게 아니라 안 떠서 댓글이 없는 것이다.** 순서를 뒤집어 댓글을 조회수 대책으로 쓰지 않는다
8. **성적 판정 기준을 조회수에서 유지율로 바꾼다.** 발행 24시간 뒤 Studio에서 `계속 시청함`을 적어 둔다. 60% 미만이면 다음 편에서 첫 3초를 갈아엎는다. 60% 이상이 3편 연속이면 피드 배급이 돌아온다
9. **사람을 넣으면 사람이 사라지는 컷은 절대 발행하지 않는다 (2026-09-23 사용자 지시, 예외 없음).**
   몸이 투명해지거나, 흐려지거나, 팔·손만 남거나, 머리·몸통이 잘리거나, 프레임 밖으로 빠지는 컷은 전부 "사라짐"이다.
   불가능한 침대 #2의 꿀 클립이 3초부터 몸이 투명해지며 팔만 남았다 — 이 현상을 다시 내보내지 않는다.
   - **프롬프트 (사람이 나오는 클립 전부 필수):**
     - 자세: 소재 더미 "안"이 아니라 가장자리에 앉히고 몸 전체를 소재 밖에 둔다(`sitting on the wooden edge of the bed frame, feet flat on the floor, the whole body clear of the ...`)
     - 복장 명시(`fully dressed in loose long-sleeved pyjamas and long trousers`)
     - 동작은 손만 짧게 반복한다. 몸을 들어 올리기·굴러가기·일어나 떠나기·뛰어들기·소재 속으로 파묻히기는 쓰지 않는다
     - 아래 문장을 그대로 넣는다:
       `The same person stays fully solid, opaque and completely visible from head to feet in every frame from the first to the last; the body never fades, never turns transparent, never dissolves, never shrinks to arms or hands only and never leaves the frame.`
   - **검수 (생성 직후, 편집 전):** 사람이 나오는 클립은 전부 1초 간격 10프레임 그리드(`raw/*_grid.png`)를 뽑아 **모든 프레임**에서 머리부터 발까지 불투명한 전신이 보이는지 확인한다.
     한 프레임이라도 걸리면 그 클립은 탈락이다. 사라지기 전 구간만 잘라 쓰는 것도 금지한다
   - **탈락 클립 처리:** 같은 프롬프트로 재생성하거나 그 아이템을 뺀다. 발행 전 최종본(`out/short.mp4`)도 클립마다 프레임을 다시 뽑아 한 번 더 확인한다


### 크레딧 현실 (v2 적용 시)

60~90초 = 아이템 6~9개 = 클립 6~9개 = 90~135크레딧. 본계정 하루 50으로는 **하루 1편이 안 나온다.**
대응: 이틀치 크레딧으로 1편(격일 발행), 또는 한 클립을 여러 아이템에 재사용(같은 방 안에서 오브젝트만 교체하는 프롬프트로 한 클립에 2~3아이템을 담는다). 매일 발행을 위해 30초 3클립으로 돌아가지 않는다 — 그 포맷이 방금 10~145회를 냈다

---

---

## 발행 이력 (중복 소재 방지)

3단계·숏폼 2번 규칙("기존 영상과 안 겹치는 조합")을 지키려면 여기부터 본다. 채널 **SliceverseAI**.

| 날짜 | 소재 | 포맷 |
|---|---|---|
| 2026-09-06 | 정글 셸터 · 대나무 수로 · 이끼 침대 · 진흙 화덕 | 부시크래프트 빌드 4편 |
| 2026-09-12 | 스퀴시(왁스 팝 · 파우치 프레스 · 모양 스퀴즈 · 크랙 스퀴즈 · 하트와 별) | 스퀴시 5편 |
| 2026-09-12 | 코코넛 크랙 & 스쿱 | 커팅 |
| 2026-09-13 | 키네틱 샌드 커팅 | 커팅 |
| 2026-09-11 | **유리 키위** | 글래스 커팅 |
| 2026-09-14 | **유리 석류** | 글래스 커팅 |
| 2026-09-15 | **유리 용과** | 글래스 커팅 |
| 2026-09-16 | 크리스털 케이크 3종(자수정 · 허니콤 호박 · 오팔) | 선택형 커팅 |
| 2026-09-16 | 얼음 과일 3종(수박 · 오렌지 · 포도송이) | 선택형 커팅 |
| 2026-09-16 | 비누 3종(라벤더 마블 · 민트 마블 · 앰버 글리세린) | 선택형 셰이빙 |
| 2026-09-16 | 허니콤 캔디 3종(골든 · 초콜릿 코팅 · 라즈베리) | 선택형 커팅 |
| 2026-09-16 | 용암 3종(사과 · 다이아몬드 · 하트) | 선택형 커팅 |
| 2026-09-17 | 보석 하이힐 3종(루비 · 에메랄드 · 사파이어) | 선택형 커팅 |
| 2026-09-17 | 크리스털 침대 3종(아쿠아마린 · 로즈쿼츠 · 시트린) | 선택형 커팅 |
| 2026-09-17 | 유리 디저트 3종(일본 말차 모찌 · 프랑스 크루아상 · 이탈리아 젤라또 콘) | 선택형 커팅(국가 테마) |
| 2026-09-17 | 크롬 과일 3종(사과 · 바나나 · 석류) | 선택형 커팅(리퀴드 메탈) |
| 2026-09-17 | 은하 레진 큐브 3종(보라 성운 · 청록 오로라 · 앰버 일몰) | 선택형 커팅(레진) |
| 2026-09-18 | 젤리 드림하우스 3종(딸기 맨션 · 청포도 성 · 오렌지 트리하우스) | 선택형 누르기(구미 젤리) |
| 2026-09-18 | 방울 스퀴시 과일 3종(수박 · 포도 · 복숭아) | 선택형 누르기(필름 속 방울) |
| 2026-09-19 | 마법 슬라임 소파 3종(딸기 크림 · 바다 오로라 · 미드나잇 갤럭시) | 선택형 누르기(슬라임) |
| 2026-09-19 | 푸드 슬라이드 3종(딸기 젤리 · 망고 크림 · 블루베리 푸딩) | 슬라이드 ASMR |
| 2026-09-20 | 푸드 슬라이드 2탄(바나나 초콜릿 퐁당 · 키위 말차 크림 · 복숭아 요거트) | 슬라이드 ASMR #2 |
| 2026-09-20 | 드림 베드 3종(선셋 클라우드 · 심해 수족관 돔 워터베드 · 우중 트리하우스) | 선택형 ASMR ("Which Bed Would You Sleep In Tonight?") |
| 2026-09-21 | 왁스 스퀴시 3종(민트 · 복숭아 · 라벤더) | 선택형 누르기(왁스 껍질) |
| 2026-09-21 | 젤 비즈 · 글리터 슬라임 · 허니 | 선택형 누르기(프레스) |
| 2026-09-21 | 푸드 슬라이드 3탄(수박+코코넛밀크 · 멜론소다+라임 · 포도+타로크림) | 슬라이드 ASMR #3 |
| 2026-09-21 | 리얼 vs 스퀴시 3종(글레이즈드 도넛 · 연어 초밥 · 아보카도) | 선택형 누르기(리얼 vs 스퀴시) |
| 2026-09-22 | 불가능한 침대 6종(수박 · 식빵 · 해파리 · 초콜릿바 · 귤젤리 · 솜사탕) | 선택형 누르기(v2 첫 편, 60초) |
| 2026-09-23 | 프루트 베이비 3종(파인애플 · 초록 사과 · 체리) | 선택형 누르기/먹기(캐릭터, 18초) |
| 2026-09-24 | 프루트 베이비 2탄(수박 · 오렌지) | 선택형 먹기(캐릭터, 12초 2클립) |

글래스 커팅 다음 후보(단면 페이오프 순): 무화과 · 스타프루트 · 패션프루트 · 오렌지 · 수박.
이 후보 목록은 **트렌드 조사에서 글래스 커팅이 계속 1등일 때만** 쓴다. 1등 포맷이 바뀌면 그쪽 소재로 새로 판다.

### 2026-09-15 실행 기록 — 유리 용과

- 결과물: https://youtube.com/shorts/nBypELEQ4I8
- 원본 3클립 라우드니스 -34.5 / -27.4 / -36.8 LUFS, LRA 3.1 / 3.3 / 7.0 — 무음 리드인 없음
- **클립 간 편차 조정은 사실상 매번 필요하다.** 기본값으로 돌리니 2.4 dB 벌어져 탈락.
  `GAIN2` 는 건 만큼 안 내려간다(리미터 때문에 약 0.55배): -2.2 dB → 1.2 dB 감소.
  실측으로 `GAIN2=-3.7` 에서 편차 **0.7 dB**로 수렴. 최종 -20.4 LUFS / LRA 4.1 / 30.0초
- 새 작업 머신(Mac)에는 ffmpeg가 없어서 `brew install ffmpeg` 부터 해야 했다. numpy는 없지만 글래스 커팅은 `detone.py`를 안 쓰니 무관

### 2026-09-16 실행 기록 — 크리스털 케이크 (첫 트렌드 추종본)

- 결과물: https://youtube.com/shorts/dDZo24XgQ5o
- 트렌드 조사 결과 1등은 **선택형("Which X would you choose?")**. 이번 주 AI 숏폼 최고 속도가 젤리 침대 3.5만이었고,
  글래스 과일 커팅 신작은 같은 주에 10편 넘게 올라왔는데 전부 1~2천에 그쳤다 → 소재를 바꿨다.
  선택 기믹만 가져오고 커팅(채널 정체성)은 유지해서 3클립을 3막이 아니라 **선택지 1·2·3**으로 썼다
- 원본 3클립 -33.2 / -33.9 / -29.6 LUFS, LRA 7.2 / 4.3 / 5.7. 무음 리드인 없음(첫 2초가 전체 평균보다 큼)
- **GAIN 보정 없이 통과한 첫 편.** 기본값으로 -20.0 / -19.9 / -20.9 LUFS(편차 1.0 dB), 최종 -20.1 LUFS / LRA 4.8 / 30.0초.
  세 클립이 같은 소재·같은 바닥이면 편차가 잘 안 벌어진다
- 부계정에서 3클립 45크레딧 소모(105 → 60). Flow 설정 화면에 **Omni 1.3 Flash**가 새로 떠 있고 기본값이지만, 검증된 1.1로 생성했다
- 브라우저 업로드 도구는 **세션이 읽을 수 있는 폴더의 파일만** 올린다. 바탕화면 경로는 거부당해서 작업 폴더로 복사한 뒤 올렸다
- Studio 자막 화면에서 **언어를 연달아 추가하면 두 번째부터 씹힌다.** 한 번에 하나씩 추가하고 목록으로 확인할 것.
  번역 편집창의 `게시` 버튼도 첫 클릭이 씹혀서 두 번 눌러야 닫혔다

### 2026-09-16 실행 기록 2 — 얼음 과일 (무료 계정 생성)

- 결과물: https://youtube.com/shorts/FJaC2Vb_6tw
- 같은 날 2편째라 트렌드 조사는 재사용하고 소재만 바꿨다(선택형 포맷 유지, 크리스털 → 얼음)
- **무료 티어 계정에서도 Omni 1.1 Flash가 뜬다.** 하루 50 크레딧 리셋이라 3클립(45)이 정확히 한 번 들어간다
- 원본 -31.5 / -30.4 / -29.2 LUFS, LRA 4.2 / 3.1 / 3.3. 기본값으로 돌리니 3번 클립 피크가 **+0.16 dBFS로 클리핑**,
  `GAIN3=-1.5` 로 -2.3 dBFS까지 내렸다. 최종 -19.3 LUFS / LRA 4.5 / TP -0.9 dBTP / 30.0초
- **라우드니스가 맞아도 피크는 따로 본다.** 빌드 스크립트가 찍는 Peak level이 0에 가까우면 그 클립만 GAIN을 내린다

#### 에이전트 UI (무료 계정 쪽 레이아웃)

- 설정은 프롬프트 상자 옆 `설정` → **동영상 생성 기본값**에서 9:16 / x1 / Omni 1.1 Flash 를 고르고 `저장`
- 생성마다 "15 크레딧을 사용하여 1개 동영상 생성을 시작하시겠습니까?" 승인 카드가 뜬다. `승인`을 누른다
- 다운로드는 미디어 타일에 마우스를 올려 `⋮` → `다운로드` → **720p 원본 크기**. zip이 아니라 mp4가 그대로 떨어진다
- 에이전트가 생성 중이면 프롬프트가 씹히니, 응답이 돌아온 뒤에 다음 프롬프트를 보낸다

#### 자막 번역 창 함정 (2026-09-16 추가)

- **편집창이 열리기 전에 타이핑하면 내용이 통째로 사라진 채 게시된다.** 창이 뜬 것을 스크린샷으로 확인한 뒤 입력하고,
  `게시` 직전에 입력 내용이 남아 있는지 한 번 더 본다. 일본어에서 빈 채로 게시될 뻔했다

### 2026-09-16 실행 기록 3 — 비누 셰이빙 (조용한 소재용 체인)

- 결과물: https://youtube.com/shorts/VkOyock4_uE
- 계정 nwgreen1026(무료, 50 크레딧). 새 프로젝트의 **동영상 기본 모델이 Veo 3.1 - Lite라 매번 Omni 1.1 Flash로 바꿔야 한다**
- 승인 카드에서 **`항상 승인`을 누르면 나머지 클립은 카드 없이 진행된다.** 3클립 연속 생성이면 이쪽이 조작이 적다
- **원본이 -48.1 / -45.1 / -49.0 LUFS로 아주 조용했다.** 비누 셰이빙은 유리·얼음보다 20 dB 가까이 작다.
  빌드 스크립트 기본 체인으로는 -31 / -26 / -29 까지밖에 안 올라와 탈락이다

조용한 소재는 아래 체인으로 따로 뽑는다(실측 -21.4 / -21.1 / -21.6 LUFS, 편차 0.5 dB, 인코딩 후 피크 -0.9 dBFS):

```bash
ffmpeg -nostdin -i raw/sN.mp4 -vn   -af "highpass=f=90:p=2,afftdn=nr=16:nf=-50,dynaudnorm=f=150:g=25:p=0.9:m=40:s=6,volume=<7.0|5.0|7.7>dB,alimiter=limit=0.7:attack=5:release=60:level=disabled,afade=t=in:st=0:d=0.04,afade=t=out:st=9.96:d=0.04"   -ar 48000 aN.wav -y
```

- `dynaudnorm` 을 `g=25:m=40` 으로 키워도 -25~-28까지만 온다. 나머지는 `volume` 으로 올리고 리미터를 **0.7**로 연다
  (비누는 유리처럼 조밀한 트랜지언트가 아니라 AAC 인코딩 후 피크 상승이 2 dB 안쪽이다)
- 영상 단계는 빌드 스크립트와 같다: `delogo=x=574:y=1138:w=56:h=56` + 1080x1920 + CRF 18 + 24fps
- 최종 -21.2 LUFS / LRA 9.5 / TP -0.6 dBTP / 30.0초. **LRA가 9.5까지 뜬다** — 탈락선(10)에 붙으니 이 소재는 매번 확인할 것

### 2026-09-16 실행 기록 4 — 허니콤 캔디 (본계정)

- 결과물: https://youtube.com/shorts/_FK7zyJEfaM
- 본계정도 새 프로젝트 기본 모델이 **Veo 3.1 - Lite**였다. 계정과 무관하게 매번 Omni 1.1 Flash로 바꾼다
- 원본 -28.8 / -35.4 / -30.8 LUFS. **초콜릿 코팅 클립만 원본 LRA가 13.5**였는데 빌드 체인을 거치면서 문제없이 내려왔다
- 기본값 빌드에서 클립 간 편차가 **3.8 dB**로 벌어져 탈락(-17.0 / -20.8 / -19.3).
  `GAIN1=-4.5 GAIN2=1.3` 으로 -19.6 / -20.5 / -19.3, 편차 **1.2 dB**. 최종 -19.6 LUFS / LRA 4.7 / TP -2.0 dBTP / 30.0초
- **감쇠는 건 만큼 거의 그대로 내려간다**(-4.5 dB → -2.6 dB 감소). 9/15에 적은 0.55배는 리미터에 물린 클립 얘기고,
  피크에 여유가 있으면 비율이 더 크다. 한 번 돌려 보고 실측으로 맞추는 게 빠르다
- 업로드 창에서 **설명란 좌표가 제안 해시태그 칩 때문에 아래로 밀린다.** 제목을 넣은 뒤 스크린샷으로 설명란 위치를 다시 확인하고 클릭할 것

### 2026-09-16 실행 기록 5 — 용암 절단 (부계정 hoohihi123123)

- 결과물: https://youtube.com/shorts/nNcIYI51yFA
- 트렌드 조사: 이번 주 AI ASMR 최고 속도가 "Which Crazy Bed Would You Choose" 6.4만/3일(약 2.1만/일).
  7일 100만 돌파본 없음. 선택형이 계속 1등이라 포맷 유지하고 소재만 바꿨다. 글래스 과일은 이미 3편이라 제외
- **직접 생성 모드는 프롬프트를 그대로 쓴다.** 프롬프트 상자에서 `동영상` 탭을 고르면 에이전트를 안 거치므로
  `Generate exactly one 10-second video ...` 래퍼가 필요 없다. 새 프로젝트 기본값은 이미지 모델(Nano Banana 2)이라
  `동영상` → `9:16` → `10초` 를 매번 고른다(모델은 Omni 1.1 Flash로 이미 잡혀 있었다)
- **생성 중에도 다음 프롬프트가 들어간다.** 직접 생성 모드는 에이전트와 달리 3클립을 연속으로 보내 병렬 생성했다. 3클립 약 2분
- 다운로드 아이콘은 클립 카드 우측에 바로 있다(⋮ 메뉴 없음). zip으로 떨어지고 순서는 최신이 위다
- 원본 -27.0 / -29.0 / -35.0 LUFS, LRA 1.4 / 0.9 / 3.7 — 용암은 크랙이 촘촘해 LRA가 매우 낮다.
  기본값 빌드가 -17.0 / -17.0 / -17.3 으로 **목표(-20)보다 3 dB 높게** 나왔다. 원본이 예전 편들보다 컸기 때문이다.
  `GAIN1=-3 GAIN2=-3 GAIN3=-2.7` 로 -19.2 / -19.2 / -19.1(편차 0.1 dB). 최종 -19.0 LUFS / LRA 6.2 / TP -1.4 dBTP / 30.0초
- **Studio 설명란에서 Escape를 누르면 업로드 창이 통째로 닫힌다.** 해시태그 자동완성만 닫으려다 창이 닫혔다.
  초안은 제목·설명까지 저장되니 콘텐츠 → Shorts → `초안 수정` 으로 이어가면 된다. 해시태그는 설명 마지막 줄에 두고 Escape 대신 창을 그대로 두고 다음 단계로 넘어가는 편이 안전하다
- 이 Mac에는 numpy가 없어 `tonecheck.py` 를 못 돌렸다. 용암은 크랙·지글 소재라 `detone.py` 대상이 아니라 그대로 진행했다

### 2026-09-17 실행 기록 6 — 보석 하이힐 (새 계정 nwgreen1024)

- 결과물: https://youtube.com/shorts/zMaXZIHkIg8
- 트렌드 조사: 이번 주 1등은 여전히 선택형. Quiz Lamby "둘 중 하나를 고르라면 - 말랑 럭셔리 하우스" 18만/4일(4.5만/일),
  DREAM DESIGN AI "Which Crazy Bed #6" 7.6만/3일(2.5만/일)로 같은 채널이 연작을 이어가는 중.
  7일 100만 돌파본 없음. 새 신호로 **탄생석/보석 테마**가 일본 채널 2곳에서 동시에 상위(1.5만/4일, 8.6천/4일)라
  선택형 + 보석 + 커팅으로 교차해 소재를 잡았다
- **유튜브 검색 필터는 "이번 주 + 조회수순"(`sp=CAMSBAgDEAE%3D`)으로 걸면 결과가 2~3개로 쪼그라든다.**
  `ai asmr shorts`, `which would you choose asmr` 처럼 질의를 바꿔 여러 번 돌려야 표가 채워진다.
  검색 결과 페이지는 스크린샷이 비어 보이는 일이 잦으니 `get_page_text` 로 읽는다
- **계정 전환은 `flow.google.com/u/<N>/` 로 한다.** 크롬에 여러 구글 계정이 로그인돼 있으면 Flow는 기본 계정으로 열린다.
  `myaccount.google.com` 으로 인덱스를 확인한 뒤 `labs.google/fx/ko/tools/flow?authuser=N` 로 들어가면 해당 계정으로 잡힌다.
  nwgreen1024는 무료 티어라 하루 50 크레딧, 3클립(45)이 한 번 들어간다
- 이 계정 UI에는 **직접 생성(동영상 탭)이 없다.** 프롬프트 상자 옆 아이콘은 미디어 첨부(+)와 에이전트 설정뿐이라
  에이전트 모드로 갔고 `Generate exactly one 10-second video ...` 래퍼가 필요했다. 첫 승인 카드에서 `항상 승인`을 눌러 나머지는 카드 없이 진행
- 새 프로젝트 기본값이 **16:9** 였다. 에이전트 설정 → 동영상 생성 기본값에서 9:16으로 바꾸고 저장(모델은 Omni 1.1 Flash로 이미 잡혀 있었다)
- 다운로드는 타일 호버 → `⋮` → 다운로드 → 720p 원본 크기. zip이 아니라 mp4가 그대로 떨어진다
- 원본 -31.7 / -33.4 / -28.4 LUFS, LRA 2.4 / 2.7 / 6.1. 첫 2초 RMS가 전체 평균보다 커서 무음 리드인 없음
- 기본값 빌드가 -20.4 / -19.9 / -18.7(편차 1.7 dB)로 탈락. `GAIN3=-3.3` 으로 -20.0까지 내렸다.
  **이 체인의 게인 반응은 0.4배다**(-3 dB → -1.2 LU). 리미터에 물린 클립은 건 만큼 안 내려가니 실측으로 맞춘다
- **s1에 `GAIN1=1.0` 을 걸었더니 라우드니스는 맞는데 인코딩 후 피크가 +0.33 dBFS로 클리핑났다.**
  0.7 dB 올렸는데 피크는 2.25 dB 뛴다. 게인을 0으로 되돌려 -1.08 dBFS / 편차 0.5 dB로 마감
- 최종 -20.1 LUFS / LRA 4.6 / TP -0.8 dBTP / 30.0초 / 1080x1920 24fps
- 업로드 파일은 **세션 스크래치패드로 복사**해서 올렸다(바탕화면 경로는 브라우저 업로드 도구가 거부한다)
### 2026-09-17 실행 기록 7 — 국가별 유리 디저트 (본계정 gksgytjr1027)

- 결과물: https://youtube.com/shorts/AMQWxZh9ICA
- 트렌드 조사: 이번 주 100만/7일 돌파본 없음. 선택형이 계속 1등이고, 그 안에서 **"국가별 푸드 머신"** 변주가
  9개 채널로 동시 확산(최고 13,609/2일 ≈ 6.8천/일). **글래스 과일 커팅은 끝물** — 이번 주 상위가 515 / 505 / 342 / 285 조회.
  그래서 국가 테마(트렌드)만 빌리고 페이오프는 커팅(채널 정체성)으로 유지했다
- 검색 결과는 DOM 대신 `window.ytInitialData` 를 읽는 게 빠르고 정확하다. 검색 URL의 `sp=CAMSBAgDEAE%3D` 가 "이번 주 + 조회수순"
- 재질 이탈: 모찌만 프롬프트대로 유리로 나왔고, **크루아상·젤라또는 실제 음식에 유리 껍질·파편이 붙은 형태**로 나왔다.
  크레딧이 5개 남아 재생성은 불가. 결과적으로 "유리 코팅 디저트 커팅"으로 일관은 유지된다
- **빌드 스크립트 기본값(limit=0.45)이 이 소재에서 클리핑났다.** 유리 디저트는 AAC 인코딩 후 피크가 **약 7 dB** 밀려 올라간다
  (wav 단계 -6.9 dBFS → 먹싱 후 0.0 dBFS). 9/11에 적은 "약 5 dB"보다 크다
- 대응: 리미터 천장을 **0.35**로 내리고 `GAIN1=2 GAIN2=1 GAIN3=1`.
  결과 -22.0 / -21.0 / -21.4 LUFS(편차 1.0 dB), 피크 -1.5 / -2.8 / -3.1 dBFS.
  최종 -21.3 LUFS / LRA 6.5 / TP -0.9 dBTP / 30.0초.
  천장을 더 내려도(0.22) 피크는 -4.9 에서 멈추고 라우드니스만 죽으니 0.30~0.35 가 이 소재의 균형점이다
- 스크립트는 그대로 두고 `sed` 로 `alimiter=limit=${LIMIT:-0.45}` 로 바꾼 로컬 사본을 썼다. 천장 조절이 반복되면 스크립트에 넣는다
- 원본 -32.0 / -27.6 / -28.9 LUFS. 무음 리드인 없음

### 2026-09-17 실행 기록 7 — 크리스털 침대 (nwgreen1026)

- 결과물: https://youtube.com/shorts/Gf9jSTN4W10
- 같은 날 2편째라 트렌드 조사는 재사용했다. 선택형 1등 유지, 그중 속도가 가장 빠른 소재가 "침대 고르기"라
  **침대 기믹 + 커팅(채널 정체성)** 으로 교차했다. 보석 테마는 앞 편에서 이어간다
- 계정 nwgreen1026은 `flow.google.com/u/3/`. 인덱스는 계정마다 다르니 `authuser=N` 을 바꿔 가며 확인한다
- 새 프로젝트 기본 모델이 또 **Veo 3.1 - Lite** 였고 비율도 16:9였다. 에이전트 설정에서 9:16 + Omni 1.1 Flash로 매번 바꾼다
- **생성 큐가 밀리면 3~4분씩 "대기열에 추가됨" 으로 멈춰 있다.** 그동안 다음 프롬프트를 보내면 병렬로 들어간다.
  다만 에이전트가 `에이전트가 실패했습니다` 를 띄우는 일이 있는데 `다시 시도` 한 번이면 복구된다(크레딧은 안 나간다)
- 원본 -29.2 / -35.7 / -30.4 LUFS, LRA 3.2 / 6.1 / 2.8. s1·s3는 **원본 True Peak가 이미 +0.1 / +0.6 dBTP** 였다
- **게인 반응이 클립마다 다르다.** 같은 빌드에서 s1은 -3 dB를 걸어도 0.4 LU밖에 안 내려갔고(-8 dB에서 4.6 LU 급락),
  s3는 거의 1:1로 내려갔다. 리미터에 물려 있는 동안은 감쇠가 거의 먹지 않다가 빠져나오는 순간 기울기가 튄다.
  **두 점을 재서 보간하는 게 제일 빠르다**(예: -3 → -19.4, -8 → -24.0 이면 1 dB당 0.92 LU)
- 최종 `GAIN1=-4.7 GAIN2=1.0 GAIN3=-4.6` 으로 -20.7 / -21.4 / -21.3(편차 0.7 dB).
  전체 -20.9 LUFS / LRA 6.1 / TP -0.3 dBTP / 30.0초

### 2026-09-17 실행 기록 8 — 크롬 과일 (부계정 hoohihi123123)

- 결과물: https://youtube.com/shorts/Wwo1c5mFMvU
- 소재 선정: 같은 날 3편째라 트렌드 조사는 재사용했다(선택형 1등 유지, 국가·보석 테마가 확산 중).
  재질이 겹치지 않는 쪽으로 **폴리시드 리퀴드 메탈**을 새로 팠다. 프롬프트 이탈 없이 3클립 모두 크롬으로 나왔다 —
  유리보다 모델이 안정적으로 재현한다
- **부계정 전환은 `labs.google/fx/ko/tools/flow?authuser=1`.** 열리면 URL이 `flow.google.com/u/1/` 로 바뀐다. 잔량 105 → 45 소모 후 60
- 이 계정 UI는 **프롬프트 상자 우측의 설정 칩**(`동영상 · 720p · 8초 · 9:16 x1`)을 눌러 바꾼다. 에이전트 설정 패널이 아니다.
  **새 프로젝트 기본값이 8초라 매번 10초로 바꿔야 한다**(8초 12크레딧 / 10초 15크레딧)
- 직접 생성 모드라 `Generate exactly one 10-second video ...` 래퍼는 불필요하다
- **생성이 끝나면 Flow가 클립 편집 화면으로 자동 전환한다.** 그 상태에서 프롬프트를 치면 편집 지시로 들어가거나 통째로 씹힌다.
  좌상단 `←` 로 프로젝트 화면에 돌아온 뒤 프롬프트 상자를 다시 클릭하고, 스크린샷으로 글자가 박혔는지 확인하고 보낼 것
- 프롬프트 상자 옆 `에이전트` 칩을 잘못 누르면 직접 생성 모드가 꺼지고 설정 칩이 사라진다. 한 번 더 눌러 되돌린다
- **"Google Flow 크레딧이 부족합니다" 배너는 잔량 60~90에서도 상시로 뜬다.** 실제 잔량은 아바타 메뉴로 확인할 것
- 원본 -34.9 / -38.6 / -33.7 LUFS. 유리(-27~-32)보다 조용하고 비누(-45~-49)보다는 크다
- 기본값 빌드가 -18.4 / -22.3 / -21.0(편차 3.9 dB)로 탈락. `GAIN1=-2.5 GAIN2=2.5 GAIN3=0.7` 로 -19.5 / -20.4 / -20.7(편차 1.2 dB).
  **금속은 피크 여유가 크다** — 유리와 달리 인코딩 후 피크가 -5 ~ -6 dBFS라 리미터 천장 0.45 기본값 그대로 써도 클리핑나지 않는다
- 최종 -20.2 LUFS / LRA 6.2 / TP -3.1 dBTP / 30.0초 / 1080x1920 24fps

### 2026-09-17 실행 기록 9 — 은하 레진 큐브 (부계정 hoohihi123123)

- 결과물: https://youtube.com/shorts/kovHcSs-JwM
- 같은 날 4편째. 트렌드 조사는 재사용하고, 그날 이미 올린 유리·크롬·보석과 **재질이 겹치지 않는 쪽**으로만 골랐다.
  하루에 여러 편을 올릴 때는 재질을 돌려야 피드에서 자기 영상끼리 안 겹친다
- 프롬프트 이탈 없음. 3클립 모두 별가루가 박힌 반투명 레진으로 나왔다. 검은 슬레이트 + 내부 발광이라 첫 프레임 대비가 세다
- **레진 크랙은 이 채널 소재 중 AAC 오버슈트가 가장 크다.** 기본값(limit=0.45)에서 s1 피크가 **+6.1 dBFS**까지 튀었다
  (유리 디저트는 0.0, 금속은 -4 수준). 같은 "쨍한 소재"라도 편차가 크니 매번 실측할 것
- 천장을 낮춰도 한계가 있다: 0.32 에서 -0.03 dBFS(여전히 위험), **0.30 에서 -1.6 dBFS**로 안정.
  0.18 까지 내리면 피크는 -8.5 인데 라우드니스가 -26.7 로 죽는다
- 최종 설정 `LIMIT=0.30 GAIN1=-2 GAIN2=2 GAIN3=3` → -22.4 / -23.0 / -23.8 LUFS(편차 1.4 dB), 피크 -1.6 / -5.0 / -3.8 dBFS
- 최종 -22.9 LUFS / LRA 5.8 / TP -1.4 dBTP / 30.0초 / 1080x1920 24fps.
  **목표(-20)보다 3 dB 조용하지만 이 소재에서는 이게 상한이다.** 더 올리면 리미터를 열어야 하고 그러면 인코딩 후 클리핑난다
- 원본 -30.5 / -32.0 / -36.4 LUFS. 무음 리드인 없음
- 부계정 크레딧 60 → 15

### 2026-09-18 실행 기록 10 — 젤리 드림하우스 (nwgreen1024, 커팅 아닌 누르기)

- 결과물: https://youtube.com/shorts/lLTJfv_2v_U
- 트렌드 조사: 선택형이 5주째 1등. 이번 주 속도 1위가 Quiz Lamby "말랑 럭셔리 하우스" 185,272/5일(37.0천/일),
  DREAM DESIGN AI "Crazy Bed #6" 98,399/4일(24.6천/일). 7일 100만 돌파본 없음.
  **글래스 과일 커팅은 완전히 죽었다** — 이번 주 신작들이 26 / 13 조회. 탄생석 테마는 일본 채널 중심으로 확산 중(3.4천/일)
- **페이오프를 커팅에서 누르기로 바꿨다.** 1등 소재가 "말랑한 것"이라 채널 정체성(커팅)을 고집하지 않고 롱폼 니치(스퀴시 누르기)를 숏폼으로 끌어왔다
- 프롬프트 이탈 없음. 3클립 모두 반투명 구미 젤리 집으로 나왔고 손이 10초 내내 움직였다
- **원본이 -40.8 / -43.1 / -46.0 LUFS로 비누급으로 조용했다.** 젤리 누르기는 유리·얼음보다 15 dB 이상 작다
- 톤 검사에서 s1 129프레임/49.7 dB, s3 114프레임/64.0 dB로 벨 성분이 크게 잡혔다.
  `detone.py` 후 s1 10프레임/38.0 dB, s3 8프레임/47.5 dB로 크게 개선 — 이 소재는 detone 필수다. s2는 3프레임이라 원본 사용
- **조용한 소재 체인(비누용)만으로는 못 올린다.** `dynaudnorm` 뒤에 게인을 아무리 걸어도 s1이 -27 LUFS에서 멈췄다.
  크레스트가 20 dB를 넘어 리미터에 계속 물리기 때문이다. 아래 컴프레서를 `dynaudnorm` 과 `volume` 사이에 넣어야 올라온다:

```
acompressor=threshold=-35dB:ratio=10:attack=5:release=250:makeup=14
```

- 이 체인에서 최종 `LIMIT=0.5 GAIN1=6 GAIN2=-5.5 GAIN3=3` → -21.4 / -22.0 / -21.1 LUFS(편차 0.9 dB), 피크 -5.4 ~ -6.0 dBFS.
  **컴프레서를 거치면 AAC 오버슈트가 작아진다**(유리·레진은 5~7 dB, 여기는 0.3 dB 수준)
- 최종 -21.2 LUFS / LRA 3.3 / TP -2.8 dBTP / 30.02초 / 1080x1920 24fps
- 클립 경계(10.0~10.4초)에 -55 dB짜리 0.4초 저음구간이 생긴다. s2 원본 도입부가 조용해서 생긴 것이고, 레벨이 내려가는 방향이라 그대로 뒀다
- 계정 nwgreen1024는 **authuser=4**. `studio.youtube.com/u/4/` 는 "오류가 발생했습니다"가 뜬다.
  `youtube.com/account?authuser=4` 로 세션을 먼저 잡은 뒤 `studio.youtube.com/` 으로 들어가면 정상적으로 열린다(채널 UCPIWmAeY05bOdFAa8OXyQfg)
- 업로드 다이얼로그가 창 밖으로 잘려 스크린샷 좌표로는 조작이 안 됐다. **`find` 로 ref를 받아 ref 클릭으로 진행**하면 잘린 영역도 그대로 조작된다
- 해시태그 자동완성은 Escape 대신 **다이얼로그 빈 공간을 클릭해 닫았다**(Escape는 업로드 창을 통째로 닫는다)

### 2026-09-18 실행 기록 11 — 방울 스퀴시 과일 (SliceverseAI)

- 결과물: https://youtube.com/shorts/3DaXRAUocl4
- 같은 날 2편째. 트렌드 조사 재사용(선택형 1등, 말랑 소재 속도 최고). 1편(젤리)과 재질·소리가 갈리도록 필름 속 방울 소재로 돌렸다
- 프롬프트 금지어에 **`no crinkling plastic`을 추가**했다. 투명 필름 소재라 안 넣으면 비닐 바스락이 붙는다. 3클립 모두 이탈 없음
- **`detone.py`가 이 소재에서는 역효과다.** 원본 톤 33 / 21 / 59 프레임이 처리 후 11 / 76 / 73으로, 3개 중 2개가 악화했다.
  방울 공명이 좁은 대역이라 detone이 벨로 오인한다. **세 클립 다 원본 오디오로 갔다**
- 원본 -37.9 / -39.4 / -36.3 LUFS. 젤리(-41~-46)보다 크고 유리(-28~-32)보다 작다
- 실행 기록 10의 체인(조용한 소재 + `acompressor=threshold=-35dB:ratio=10:makeup=14`) 그대로 쓰되 게인만 낮췄다.
  기본값이 -18.2 / -19.1 / -18.7(편차 0.9)로 이미 목표보다 2 dB 컸다. `LIMIT=0.5 GAIN1=-2.3 GAIN2=-1.4 GAIN3=-1.8` → -19.9 / -20.1 / -20.0(편차 0.2 dB).
  **감쇠 방향은 거의 1:1로 먹는다** — 리미터에 안 물리기 때문이다
- 최종 -19.7 LUFS / LRA 6.1 / TP -5.0 dBTP / 30.02초 / 1080x1920 24fps
- **빌드 스크립트의 `upload.mp4`(CRF 23)가 10.68MB로 브라우저 업로드 한도를 넘었다.** 이 소재는 방울이 많아 압축이 안 먹는다.
  CRF 26으로 다시 인코딩해 7.49MB로 맞췄다(영상 재인코딩만, 오디오는 `-c:a copy`)
- **nwgreen1026 계정에는 유튜브 채널이 없다.** Studio로 들어가면 `youtube.com/create_channel` 로 튕긴다.
  채널 **SliceverseAI(UCPIWmAeY05bOdFAa8OXyQfg)는 nwgreen1024(authuser=4)로 접근**한다. 계정 인덱스: 0=gksgytjr1027, 2=hoohihi123123, 3=nwgreen1026, 4=nwgreen1024
- 공개 상태 라디오는 ref 클릭이 안 먹는 경우가 있어 `document.querySelectorAll('tp-yt-paper-radio-button')[2].click()` 로 눌렀다

### 2026-09-19 실행 기록 12 — 마법 슬라임 소파 & 푸드 젤리 슬라이드

- 결과물:
  - 마법 슬라임 소파: https://youtube.com/shorts/iMjbqtz2egs
  - 푸드 젤리 슬라이드: https://youtube.com/shorts/v7xCxu68CzE
- 트렌드 조사: `ai video shorts` 검색에서 `Oddly satisfying AI food Slide`가 2일 만에 65만 뷰로 가장 빠른 시그널.
- 푸드 슬라이드 3클립: 딸기 젤리 슬라이드 → 망고 크림 슬라이드 → 블루베리 나선 슬라이드 & 푸딩. 9:16 / Omni 1.1 Flash.
- **채널 전수 진단 및 알고리즘 최적화 분석 수행**:
  - 채널 28개 영상 중 상위 8~9개가 1.1K~1.2K에서 정확히 정체(Seed Test 한계).
  - 원인: 30초 고정 길이로 인한 낮은 완주율(APV), 화면 내 질문 텍스트 부재로 인한 스와이프 이탈(VVSA 저하), 수면용 Bio와 숏폼 실제 콘텐츠의 괴리.
  - 개선 규칙("알고리즘 돌파 및 채널 최적화 규칙")을 수립하여 본 문서에 추가 반영.

### 2026-09-20 실행 기록 13 — 푸드 젤리 슬라이드 #2 (본계정 gksgytjr1027)

- 결과물: https://youtube.com/shorts/mBlniEf4QvI
- 포맷: 푸드 슬라이드 2탄(바나나 & 초콜릿 퐁당 · 키위 & 말차 크림 워터슬라이드 · 복숭아 & 그릭 요거트 슬라이드)
- 계정 및 모델: 본계정 `gksgytjr1027`(authuser=0, 45 크레딧 소모), **Omni 1.1 Flash**, 9:16, 10초 x1
- Flow 생성 자동화: Apple Events 활성화 상태에서 Chrome Tab DOM 제어로 모델 세팅, 프롬프트 입력, 항상 승인 및 자동 렌더/다운로드 수행
- 원본 레벨: s1 -37.7 / s2 -34.6 / s3 -23.1 LUFS
- FFT 톤 검사 및 detone:
  - s1: 28프레임/67.8 dB -> 6프레임/42.6 dB로 개선
  - s2: 149프레임/88.7 dB -> 27프레임/50.2 dB로 개선
  - s3: 107프레임/98.6 dB -> 34프레임/64.0 dB로 개선
- 게인 캘리브레이션: `GAIN1=-3.4 GAIN2=-5.2 GAIN3=-6.8` (감쇠 1:1 선형 반응 확인)
  - 개별 클립 최종: s1 -19.6 / s2 -19.7 / s3 -19.5 LUFS (**편차 0.2 dB**로 완벽 균일)
  - 피크 레벨: s1 -3.79 / s2 -5.64 / s3 -6.18 dBFS (클리핑 없음)
- 최종 마스터링(`out/short_30s.mp4`):
  - -19.6 LUFS / True Peak -3.8 dBTP / LRA 9.8 LU / 30.02초
  - 워터마크 제거 (`delogo=x=574:y=1138:w=56:h=56`) + Lanczos 1080x1920 업스케일 24fps
  - 브라우저 업로드 경량본 `out/upload.mp4` (8.1MB, 10MB 한도 충족)
- 업로드 및 첫 댓글: 제목/설명/라벨(AI 합성콘텐츠 체크)/공개 상태(Public)로 게시 완료 및 공식 첫 댓글(`I'm definitely taking the Matcha Kiwi slide! 🥝 Which one are you riding? 👇`) 작성 완료.

### 2026-09-20 실행 기록 14 — 드림 베드 3종 ("Which Bed Would You Sleep In Tonight?") (hoohihi123123)

- 결과물: https://youtube.com/shorts/osxw15WcEdo
- 포맷: 글로벌 숏폼 1위 선택형 포맷 ("Which Bed Would You Sleep In Tonight? 🛌✨")
  - Scene 1: 노을빛 파스텔 구름 침대 & 황금빛 별먼지 (Pastel Sunset Cloud Bed)
  - Scene 2: 심해 해저 수족관 돔 워터베드 & 발광 해파리 (Bioluminescent Aquarium Dome Bed)
  - Scene 3: 비 내리는 숲속 거대 나무 오두막 침대 & 청키 니트 울 이불 (Rainy Forest Treehouse Bed)
- 계정 및 모델: 구글 플로우 계정 `hoohihi123123`(authuser=1, 45 크레딧 소모), **Omni 1.1 Flash**, 9:16, 10초 x1, 720p
- Flow 생성 자동화: Chrome AppleScript DOM 제어로 Omni 1.1 Flash 모델 세팅, 프롬프트 입력 및 렌더/일괄 다운로드 수행
- 원본/필터링 레벨: s1 -16.6 / s2 -13.3 / s3 -20.6 LUFS
- 게인 캘리브레이션: `GAIN1=-3.4 GAIN2=-6.7 GAIN3=0`
  - 개별 클립 최종: s1 -20.0 / s2 -20.0 / s3 -20.6 LUFS (**최대 편차 0.6 dB**로 < 1.5 dB 한도 완벽 충족)
- 최종 마스터링(`out/short_30s.mp4`):
  - **-20.3 LUFS** (목표 -20.0 ± 1.0 dB 충족)
  - **True Peak -4.2 dBTP** (0.0 dBTP 한도 충족, 클리핑 전혀 없음)
  - 워터마크 제거 (`delogo=x=574:y=1138:w=56:h=56`) + Lanczos 1080x1920 업스케일 24fps
  - 브라우저 업로드 경량본 `out/upload.mp4` (6.6MB, 10MB 한도 충족)
- 유튜브 게시 및 메타데이터:
  - 알고리즘 씨앗 테스트 격리 방지를 위해 **100% 영문 메타데이터** 규칙 준수 (제목/설명/댓글 일체 한국어 배제)
  - 제목: `Which Bed Would You Sleep In Tonight? 🛌✨ Cloud vs Ocean vs Rain | AI ASMR`
  - 아동용 아님(Not for kids) / AI 변형·합성 콘텐츠(Yes) 체크
  - 공식 첫 댓글 작성 완료: `I'm definitely sleeping in the rainy treehouse bed tonight! 🌧️ Which one are you choosing? 👇`




### 2026-09-21 실행 기록 15 — 푸드 슬라이드 #3 (본계정 gksgytjr1027)

- 결과물: https://youtube.com/shorts/zYcWKG59enQ
- 포맷: 푸드 슬라이드 3탄(수박+코코넛밀크 · 멜론소다+라임 · 포도+타로크림). 알고리즘 규칙 #4·#5·#6을 처음으로 전부 적용한 편이다
- 트렌드 조사: 이번 주 1등이 `Oddly satisfying AI food Slide` 107만/4일(26.8만/일)로 7일 100만을 넘겼고 8개 채널로 확산 중.
  1등군은 "baby laughing" 오디오와 아기 얼굴을 얹어 키즈 시청층까지 먹지만, 무얼굴 규칙과 충돌해 그 레이어는 안 가져왔다.
  대신 실제 foley 레이어(#6)로 대체했다. 신규 변주로 "AI food bed"(10만/1일)가 올라오는 중이라 다음 후보다
- **발행 이력표가 실제 채널보다 뒤처져 있었다.** 채널 32편을 전수 확인하니 왁스 스퀴시·젤 비즈 2편이 표에 없었다.
  소재 중복 판정은 표가 아니라 **채널 Shorts 목록을 직접 읽고** 해야 한다(`youtube.com/channel/<ID>/shorts` + `get_page_text`)
- 계정/모델: 본계정 gksgytjr1027(크레딧 50 → 5), Omni 1.1 Flash, 9:16, 10초 x1, 720p. 에이전트 모드 + `항상 승인`
- **에이전트 응답이 돌아온 직후에도 전송 버튼이 정지(■)로 남아 있다.** 그 상태에서 누르면 방금 친 프롬프트가 취소된다
  (`대답이 취소되었습니다`). 버튼 아이콘을 zoom으로 확대해 화살표(→)인지 확인하고 누를 것
- 미디어 타일 메타데이터는 생성 초반에 `720p · 8초`로 잘못 떴다가 완료되면 `Omni 1.1 Flash · 720p · 10초`로 바뀐다. 8초로 보여도 바로 재생성하지 말 것
- 원본 레벨: s1 -30.9 / s2 -17.4 / s3 -44.4 LUFS. **한 편 안에서 27 dB 벌어졌다** — 소재별 편차가 이렇게 크면 GAIN 보정이 필수다
- FFT 톤 검사: s1 169프레임/63.6 dB, s2 91프레임/80.3 dB, s3 0프레임.
  **`detone.py`는 두 클립 다 탈락이다.** s1은 172프레임/67.0 dB로 악화했고, s2는 톤이 16프레임으로 줄었지만
  **광대역 RMS가 -31.2 → -44.0 dB로 12.8 dB 깎였다**(기준은 0.3 dB). 탄산 방울처럼 대역이 좁은 소리는 detone이 신호로 안 보고 지운다. 세 클립 다 원본 오디오로 갔다
- **foley 레이어링(#6) 첫 적용.** 소스는 archive.org `Designers-Choice-Collection-Water`(CC0).
  `pour_water`(크리미 슬라이드 베드) · `fizz_loop`(멜론소다) · `splash_small`(입수 악센트)를 받아
  각 클립 라우드니스 기준 **-14 dB(약 20%)**, 입수음만 -12 dB로 깔았다. 스크립트는 작업 폴더의 `mix_foley.sh`
- **레포 `scripts/build-short.sh`(1패스 loudnorm)로는 -14.5 LUFS에 못 닿는다.** 결과가 -19.1 / -13.9 / -18.8(편차 5.2 dB)에 s3는 피크 +0.26 dBFS로 클리핑.
  작업 폴더 `build2.sh`로 **2패스 loudnorm(linear) + 게인 + 리미터**를 썼다:
  `highpass=f=45:p=2, acompressor=threshold=-26dB:ratio=2.5:attack=20:release=250, loudnorm(measured_* 2패스, I=-14.5:TP=-1.0:LRA=7), volume=GAINn, alimiter=limit=0.75:level=disabled, 40ms 페이드`
  - 컴프레서는 **ratio 2.5:1 / attack 20ms**만 건다. 조용한 원본을 16 dB 넘게 올리려면 크레스트를 줄여야 하는데, 금지된 10:1 라디오 압축과 달리 첫 10~20ms 어택은 살아 있다
  - `GAIN1=6 GAIN2=-0.7 GAIN3=0.7` → -14.5 / -14.5 / -14.2 LUFS(**편차 0.3 dB**), 피크 -0.88 / -2.32 / -1.52 dBFS
  - **리미터에 물린 클립의 게인 반응은 0.53배다**(GAIN1 +4 dB → +2.1 LU). 감쇠·여유 있는 클립은 거의 1:1
- **길이 압축(#4)과 텍스트 후크(#5) 적용**: 30초 마스터를 `setpts=PTS/1.7` + `atempo=1.7`로 **17.67초**로 줄였다.
  - **이 Mac의 ffmpeg에는 `drawtext`가 없다**(`No such filter: 'drawtext'`). 텍스트는 Pillow로 1080x1920 투명 PNG 3장을 그려 `overlay` + `enable`로 얹었다. 스크립트는 `make_overlays.py` / `finish.sh`
  - 상단 후크 `Which slide are you riding?` 고정 + 씬별 `1. Watermelon Coconut` / `2. Melon Soda Lime` / `3. Grape Taro Cream`
  - 오버레이에 이모지는 넣지 않았다. Pillow가 컬러 이모지 폰트를 못 그린다 — 이모지는 제목·댓글에만 쓴다
- 최종 마스터: **-14.6 LUFS / True Peak -1.4 dBTP / LRA 4.2 LU / 17.67초 / 1080x1920 24fps**. 업로드본 `out/upload.mp4` 6.3MB(CRF 24)
- 업로드: 아동용 아님 / AI 사용 `예` / 공개. 첫 댓글 작성 완료(`I'm riding the melon soda slide! 🍈 Which one are you picking? 👇`)
- **첫 댓글 고정(Pin)은 못 했다.** 고정을 누르면 `고급 기능 이용하기` 안내가 뜬다 — 채널이 YouTube 스튜디오 본인 인증(전화 인증)을 안 거쳤다.
  인증은 사람이 직접 해야 한다. 인증 전까지 규칙 #7의 고정·투표 스티커는 실행 불가다

### 2026-09-21 실행 기록 16 — 리얼 vs 스퀴시 (부계정 hoohihi123123)

- 결과물: https://youtube.com/shorts/JUA4Rp8IQcs
- 포맷: **Real or Squishy?** — 진짜처럼 보이는 음식을 눌렀더니 스퀴시처럼 눌렸다 튀어오른다. 소재는 글레이즈드 도넛 · 연어 초밥 · 아보카도 반쪽
- 같은 날 2편째. 트렌드 조사는 재사용하되 **포맷을 바꿨다**. 1등(푸드 슬라이드)을 또 올리면 자기 영상끼리 피드에서 겹친다.
  2등 신호가 밸런스 게임 계열 "리얼 vs 스퀴시"(Quiz Dino 19만/3일 = 6.3만/일, Quiz Ninja 3.2만/일, Quiz Blitz 0.9만/일)로 3채널 확산 중이라 그쪽으로 갔다
  - `ai food bed`는 ASMR 니치 복제본이 746 / 217 / 161 조회로 죽어 있었다. 1등 채널(Baby Giggles 10만/1일)의 속도는 포맷이 아니라 아기 오디오에서 나온다고 보고 제외했다
- 계정/모델: hoohihi123123(크레딧 65 → 20), Omni 1.1 Flash, 9:16, 10초 x1, 720p. **직접 생성 모드**라 래퍼 문장 불필요, 3클립 연속 전송해 병렬 생성(약 90초)
- 원본 레벨: s1 -42.3 / s2 -35.6 / s3 -46.3 LUFS. 손가락 누르기 소재는 유리·얼음보다 15~20 dB 조용하다
- 톤 검사: s1 26프레임/53.3 dB, s2 54프레임/80.5 dB, s3 0프레임
  - **s1은 detone 적용**(26 → 0프레임, 광대역 RMS -42.80 → -43.81 dB로 1.0 dB만 손실)
  - **s2는 detone 탈락**(54 → 26프레임이지만 RMS -43.50 → -51.52 dB로 8.0 dB 증발). 적용 판단은 톤 프레임 수가 아니라 **광대역 RMS 손실**로 한다. 1 dB 안쪽이면 채택, 그 이상이면 원본
- foley(#6): 스퀴시 프레스에는 물 입수음이 안 맞아 CC0 `WATRDrip_Cloth, Wet, Wring Out`(젖은 천 짜기)을 **-16 dB**로 깔았다. 입수 악센트는 쓰지 않았다
- **원본이 -42 ~ -46 LUFS면 `acompressor` 스레숄드(-26 dB)에 신호가 닿지 않아 압축이 안 걸린다.**
  클립별로 `-20 LUFS` 근처까지 선형 프리게인(+15.9 ~ +26.3 dB)을 먼저 먹인 뒤 압축하도록 `build2.sh`를 고쳤다.
  프리게인 없이는 -20.3 / -17.0 / -16.2(편차 4.1 dB)로 탈락, 넣은 뒤 `GAIN1=7 GAIN2=2 GAIN3=0.6` 으로 **-14.6 / -14.3 / -14.3(편차 0.3 dB)**
- **ffmpeg 함정 두 개를 새로 밟았다.**
  1. **체인 `overlay`+`enable` 은 두 번째 이후 오버레이가 통째로 빠질 수 있다.** 같은 필터그래프가 9/21 1편에서는 정상 동작했는데 이 편에서는 씬 2·3 텍스트가 렌더에 아예 안 들어갔다(직접 합성하면 정상).
     **씬마다 따로 오버레이를 구운 뒤 concat** 하도록 `finish.sh`를 바꿨다. enable 표현식에 의존하지 않는다
  2. **AAC 스트림을 그대로 `atempo` 에 물리면 배속이 덜 먹는다.** 30.0초 마스터에 `atempo=1.7` 을 걸었는데 결과가 17.65초가 아니라 **21.75초**(실효 1.38배)로 나왔다.
     같은 오디오를 `pcm_s16le` 로 뽑아 물리면 17.64초로 정확하다. **배속 단계 입력은 PCM으로 뽑아 쓴다**
  3. 씬별 재인코딩에서 `-c:a copy` 로 오디오를 같이 넣으면 concat 뒤 길이가 틀어진다. 영상만 `-an` 으로 굽고 오디오는 원래 마스터에서 따로 가져온다
- 최종 마스터: **-14.3 LUFS / True Peak -1.6 dBTP / LRA 2.8 LU / 17.67초 / 1080x1920 24fps**, 업로드본 5.6MB
- 업로드: 아동용 아님 / AI 사용 `예` / 공개. 첫 댓글 작성 완료(`The nigiri one broke my brain 🍣 Which one fooled you? 👇`).
  고정(Pin)은 실행 기록 15와 같은 사유(채널 본인 인증 미완료)로 여전히 불가



### 2026-09-21 실행 기록 17 — 왁뿌볼 (왁스 체인을 숏폼에 처음 적용)

- 결과물: https://youtube.com/shorts/fntuz4cqSuo
- 최종 -21.2 LUFS / TP -2.2 dBTP / LRA 7.9 / 30.0초. 클립별 -20.9 / -22.1 / -21.3 (편차 1.2 dB)
- 설정: `GAIN1=-3.0 GAIN2=5.5 GAIN3=-7.6 LIMIT=0.35`
- **숏폼 기본 체인(dynaudnorm 단독)으로는 왁스가 안 잡힌다.** 원본 3클립이 LRA 25.7 / 9.9 / 23.7로 두 개가 탈락이었다.
  롱폼의 왁스 2단 컴프레서를 앞에 붙이고 **`loudnorm` → `dynaudnorm` 순서**로 걸어 7.9 LU까지 내렸다.
  순서가 중요하다. `dynaudnorm`을 컴프레서 바로 뒤(loudnorm 앞)에 두면 압축한 레벨을 도로 벌려놔서 LRA가 20대로 돌아온다
- "이 체인에서는 컴프레서를 쓰지 않는다"는 위 주의는 **뒤에 `loudnorm`이 없을 때** 이야기다. 메이크업을 loudnorm이 대신하면 신호가 죽지 않는다
- **리미터 0.45로는 클리핑났다.** s2 피크가 인코딩 후 -0.05 dBFS, 합본 True Peak +1.5 dBTP.
  `LIMIT=0.35`로 내려 -2.2 dBTP로 수렴. 왁스처럼 임펄스가 촘촘한 소재는 0.35를 기본으로 본다
- **`build.py`가 클립 1개 처리 후 죽었다.** `subprocess.run(..., text=True)`가 ffmpeg stderr를 cp949로 디코드하다
  `UnicodeDecodeError`. `encoding="utf-8", errors="replace"`를 붙여야 한다. 한글 Windows에서는 항상 붙일 것
- **다운로드 파일명 순서를 믿지 말 것.** Flow에서 받은 `s1/s2/s3.mp4`가 프롬프트 1/2/3 순서와 달랐고,
  raw로 복사할 때 한 클립이 중복으로 들어가 있었다. `ffmpeg -ss 3 -frames:v 1`로 프레임을 뽑아 눈으로 대조하고 넣는다
- `upload.mp4`가 CRF 23에서 10.5MB로 업로드 한도를 넘어 **CRF 26**으로 내렸다(7.4MB, 화질 차이 안 보임)
- **Studio 해시태그 자동완성은 Escape 한 번이면 충분하다.** 두 번 누르면 업로드 창이 통째로 닫힌다.
  닫혀도 초안은 저장되니 콘텐츠 → Shorts 동영상 → `초안 수정`으로 이어서 하면 된다
- 브라우저 업로드 도구는 세션 허용 폴더만 읽는다. 스크래치패드도 거부당했고, `/add-dir`로 작업 폴더를 추가해야 올라갔다



### 2026-09-21 실행 기록 18 — 유압 프레스 (격동 포맷 전환, 소리 금지어 전면 해제)

- 결과물: https://youtube.com/shorts/7CMzaXgN95k
- 최종 -20.1 LUFS / TP -1.8 dBTP / LRA 9.0 / 30.02초. 클립별 -19.0 / -19.8 / -20.3 (편차 1.3 dB)
- 설정: `LIMIT=0.5 GAIN1=-2.8 GAIN2=-1.1 GAIN3=6`, 계정 nwgreen1026(authuser=3, 무료 50 → 5)
- **격동 포맷을 처음 만들었다.** 손가락 누르기 대신 강철 프레스가 내려와 막이 터지고 내용물이 분출한다.
  숏폼은 수면용이 아니므로 파열음을 상품으로 쓴다. 소재 3종은 분출물 소리가 갈리게 잡았다(젖은 젤 구슬 · 끈적 글리터 슬라임 · 걸쭉한 꿀)
- **사용자 지시로 소리 금지어를 전부 뺐다.** `no single loud pop` · `no ringing/bell/chime` · `no metallic clang/motor whine/hiss` 전부 제거, `No music, no voice.`만 남겼다.
  결과: 벨·차임은 안 붙었고 3클립 모두 프롬프트대로 나왔다. 금지어를 빼도 재질 이탈은 없었다
- **강철 플레이트 보호 문장을 넣었다**(칼날 보호 문장의 프레스 판): 3클립 모두 플레이트에 금 안 감
- **raw LRA는 저역 럼블 때문에 부풀어 보인다.** s1 원본 LRA 24.2였는데 `highpass=f=80` 만 걸면 4.5로 떨어졌다.
  지배 톤이 47 Hz(다음이 59 · 35 · 23 Hz)였다. **원본 LRA로 체인을 고르기 전에 highpass 뒤 값을 다시 재라**
- **`detone.py`는 3클립 다 악화**(63→223 / 36프레임 43.6→48.8 dB / 4→13프레임). 전부 원본 오디오로 갔다.
  톤이 벨이 아니라 저역 럼블이면 detone은 손해다
- **컴프레서는 클립마다 켜고 꺼야 했다.** s1은 컴프레서 없이 LRA 4.5인데 걸면 22.7로 **악화**한다(릴리스가 조용한 압박 구간을 도로 끌어올린다).
  s2·s3는 반대로 컴프레서(`threshold=-35dB:ratio=10:makeup=14`)가 없으면 dynaudnorm이 다시 벌려 14.6까지 뜬다.
  빌드 스크립트에 `use_comp = [False, True, True]` 리스트를 두고 클립별로 분기했다
- **왁스 2단 컴프레서 체인을 그대로 쓰면 안 된다.** 메이크업이 없어 s1이 -61.3 LUFS로 죽었고 뒤의 `loudnorm`도 못 살렸다
- **리미터를 0.35로 조이면 게인이 안 먹는다.** 세 클립 모두 리미터에 물려 감쇠 반응이 0.21~0.37배까지 떨어져 편차가 안 좁혀졌다.
  천장을 **0.5**로 열자 반응이 거의 1:1로 돌아왔고 편차 1.3 dB로 수렴했다. 인코딩 후 피크는 -5.8 / -2.7 / -3.4 dBFS로 여유가 있었다
  (프레스 파열음은 유리·레진과 달리 AAC 오버슈트가 작다)
- 다운로드 파일명이 프롬프트 내용으로 떨어져서(`Steel_press_crushing_gel_ball_...mp4`) 순서 대조가 필요 없었다.
  이 계정 UI는 타일 ⋮ → 다운로드 → 720p, zip이 아니라 mp4가 그대로 떨어진다
- Studio 업로드는 `studio.youtube.com/channel/<ID>/videos/upload` 로 들어간 뒤 우상단 **만들기 → 동영상 업로드**를 눌러야
  `input[type=file]`이 DOM에 생긴다. 그 전에는 파일 입력이 0개라 `file_upload`가 못 붙는다
- 해시태그 자동완성은 **다이얼로그 안 빈 공간(세부정보 제목 줄) 클릭**으로 닫았다. Escape는 창을 닫는다



### 2026-09-22 실행 기록 19 — 불가능한 침대 6종 (v2 첫 편, nwgreen1024 + nwgreen1026)

- 결과물: https://youtube.com/shorts/lPReLibhAFw (제목 `Which Impossible Bed Would You Sleep In? 🛏️ #1 | AI ASMR`, 공개)

- 소재: 수박 / 식빵 / 해파리 / 초콜릿바 / 귤젤리 / 솜사탕 침대. 같은 침실·같은 창을 6클립에 고정
- 계정 배분: nwgreen1024(50크레딧)로 3클립, nwgreen1026(50크레딧)으로 3클립. 각 15크레딧
- **에이전트가 생성 중일 때 보낸 프롬프트는 정말로 통째로 씹힌다.** 식빵 클립을 병렬로 밀어 넣었다가 유실됐고,
  크레딧 잔량(50 → 20)으로 2건만 과금된 걸 확인하고서야 알았다. 승인 카드가 뜨고 결과가 나온 뒤에 다음 프롬프트를 보낸다
- 새 프로젝트 기본값이 계정마다 다르다. nwgreen1026은 **Veo 3.1 - Lite + 16:9** 로 열려서 9:16 + Omni 1.1 Flash로 바꿨다
- 프롬프트에 `A person seen from behind, face never visible` 를 넣으니 6클립 모두 얼굴 없이 뒷모습으로 나왔다. 광각에 사람을 넣어도 안면 붕괴가 없다
- 자막 굽기용 `scripts/build-short-v2.sh` 추가: 클립 수 자동 인식, 상단 질문 + 클립별 번호 + 마지막 2초 CTA, delogo·업스케일·라우드니스는 기존과 동일
- 1차 빌드가 -13.6 ~ -20.2 LUFS로 벌어져 `GAIN1=-4.5 GAIN2=-4.9 GAIN3=-6.9 GAIN4=-3.5 GAIN5=-0.3 GAIN6=-6.8` 로 재빌드 → -19.1 ~ -20.4(편차 1.3 dB)
- 최종 60.02초 / 1080x1920 / -19.4 LUFS / LRA 8.6 / TP -2.5 dBTP. 원본 29MB, 업로드용은 CRF 28로 7.6MB
  (60초는 CRF 23이면 15MB라 브라우저 업로드 10MB 한도를 넘는다)
- 업로드: 세부정보에서 **AI 사용 = 예**로 신고했다(광각 침실과 사람이 사실적이라 보수적으로 선택). 유료 프로모션 = 아니요, 아동용 = 아니요
- 업로드 다이얼로그가 이번에도 중간에 잘려 `find` 로 ref 를 받아 클릭했다. 해시태그 자동완성은 다이얼로그 빈 공간 클릭으로 닫는다


### 2026-09-22 실행 기록 20 — 불가능한 침대 #2 (2026-09-23 발행, 5클립)

- 결과물: https://youtube.com/shorts/UFq0pqBoLDA (제목 `Which Impossible Bed Would You Sleep In? 🛏️ #2 | AI ASMR`, 공개)
- **꿀 클립은 뺐다.** 3초쯤부터 몸이 투명해지고 팔만 남아 재생성 대신 5클립 50초로 발행
- foley(#6): archive.org Designer's Choice에는 뽁뽁이·낙엽·모래가 없다. **BigSoundBank(CC0, 계정 불필요)** 에서 받았다.
  `https://bigsoundbank.com/UPLOAD/mp3/<ID>.mp3` — 뽁뽁이 0462(7초라 루프), 파도+자갈 1445, 쌀 붓기 0201, 낙엽 밟기 2889, 과자봉지 1433. 전부 -16 dB
- **`scripts/build-short-v2.sh` 는 아직 구 체인(afftdn, -20.5 LUFS)이다.** 규칙 9와 충돌해 작업 폴더 `build.sh` 로 고쳐 썼다:
  `highpass=45 → acompressor 2.5:1 → 실측 후 -14.5 LUFS 자동 게인 → alimiter`, 번호 옆 아이템 이름(`LABELS="A|B|..."`), 질문 글자 크기 `QSIZE`(40자 질문은 58이면 화면 밖으로 넘친다 → 42), 업로드 CRF `UPCRF`
- `LIMIT=0.7 GAIN1=6 GAIN2=0.8` → 클립 -15.1 ~ -14.6 LUFS(편차 0.5 dB), 최종 -14.8 LUFS / TP -1.4 dBTP / LRA 3.2 / 50.02초, 업로드본 CRF 28 7.0MB
- **시청 페이지(youtube.com/watch)에서 댓글을 달면 채널 없는 계정으로 잡혀 "채널이 생성됩니다" 가 뜬다.** 첫 댓글은 `studio.youtube.com/video/<ID>/comments` 에서 달아야 SliceverseAI로 달린다

- 소재 6종: 뽁뽁이 · 거대 조개껍데기(진주+바닷물) · 키네틱 샌드 · 낙엽 · 팝콘 · 벌집 꿀.
  소리 정체를 6개 다 다르게 잡았다(팝 / 젖은 클릭 / 알갱이 / 마른 바스락 / 마른 크런치 / 끈적 스퀄치)
- 트렌드 조사: 이번 주 1등은 여전히 `Oddly satisfying AI food Slide + Baby laughing`(117만/5일 = 23.4만/일)이지만
  동력이 아기 얼굴·웃음 오디오라 무얼굴 규칙과 충돌해 제외했다. 2등도 같은 아기 계열(23만/4일).
  본채널 실측(구독 20, 35편)에서 최고는 침대 선택형 140회라 침대 시리즈를 이어간다
- 계정별 소모: gksgytjr1027 50 → 5(3클립), hoohihi123123 50 → 5(2클립 + 정책 반려 1건 재시도),
  nwgreen1026 50 → 5(3클립). **한 편에 6클립이면 계정 3개가 필요하다**

새로 밟은 함정 다섯 가지.

1. **사람이 소재 더미 "안"에 있으면 모델이 팔만 그린다.** `kneeling on the bed` / `kneeling in the leaf pile` 로 쓴 클립은
   뽁뽁이·조개·낙엽 3건 모두 몸통과 머리 없이 팔 두 개만 나왔다. 광각에서 이건 공포 영상이다.
   프롬프트를 **침대 가장자리에 앉아 상체를 숙이는 자세**(`sitting on the wooden edge of the bed frame, feet flat on the floor,
   the whole body clear of the leaves, leaning forward`)로 바꾸니 전신이 나왔다. 사람을 넣을 거면 소재에 묻지 않게 배치한다
2. **금지어에 `no disembodied or cropped limbs, no headless bodies` 를 넣는 것만으로는 안 막힌다.** 자세를 바꿔야 막힌다
3. **조개껍데기 침대 + 사람 프롬프트가 성적 콘텐츠 정책에 걸렸다**(`이 프롬프트는 성적인 콘텐츠 생성에 관한 Google 정책을
   위반할 가능성이 있습니다`). 요금은 청구되지 않는다. `A person fully dressed in loose long-sleeved pyjamas and long trousers`
   로 복장을 명시하니 통과했다. 사람을 넣는 프롬프트에는 복장을 항상 적는다
4. **`오디오를 생성할 수 없습니다` 실패가 있다.** 프롬프트를 고치지 말고 타일의 재시도(↻)를 누르면 같은 프롬프트로 통과한다.
   이 실패도 요금이 청구되지 않는다
5. **전송 버튼 한 번 누르면 두 번 발화한다.** 생성은 1건만 걸리고 곧바로 `크레딧이 부족합니다` 다이얼로그가 같이 뜬다.
   다이얼로그는 Escape로 닫고 무시한다 — 생성은 정상 진행 중이다. 잔량을 보고 재전송하면 이중 과금이 난다

기타 UI 메모.

- **계정은 `?authuser=N` 으로 바꾼다**(이 PC 기준 0=gksgytjr1027, 2=hoohihi123123, 3=nwgreen1026). 1번은 Workspace 계정이라 Flow가 막힌다
- **새 프로젝트가 계정마다 다른 모드로 열린다.** 에이전트 패널로 열리면 톱니(에이전트 설정)에서 9:16 / x1 / Omni 1.1 Flash 를 잡고,
  직접 생성 모드로 열리면 프롬프트 바의 설정 칩에서 잡는다. **직접 생성 모드 기본값은 360p · 8초라 720p · 10초로 바꿔야 한다**
- 다운로드는 타일의 `일괄 다운로드`를 누르면 zip, 편집기를 열어 우상단 다운로드 → `720p 원본 크기`를 누르면 mp4가 바로 받아진다
### 2026-09-23 실행 기록 21 — 프루트 베이비 (본계정 gksgytjr1027, 첫 캐릭터 포맷 / 첫 18초본)

- 결과물: https://youtube.com/shorts/WEZeyYWr7nM
- 트렌드 조사: **이번 주 처음으로 7일 100만 돌파본이 나왔다** — Baby Giggles "AI food Slide" 1,303,330/7일(186K/일).
  같은 포맷이 20개 이상 채널로 확산 중(Dudi Rhymes 789,977/6일 = 132K/일). 밸런스게임 46K/일, 국가별 초콜릿 15.7K/일이 그 다음.
  **Crazy Bed 연작은 끝물** — DREAM DESIGN AI #10이 9.4K/일인데 #11은 0.9K/일로 10배 꺾였다
- 1등 포맷 정체: 과일 코스튬을 입은 3D 픽사풍 **아기**가 자기와 같은 종류의 거대 과일을 안고 베어문다.
  소리 상품은 과즙 크런치 + **아기 웃음**(1등 영상 제목에 `Baby laughing sound`가 박혀 있다)
- **규칙을 고쳐서 갔다.** 얼굴이 이 포맷의 스크롤 정지 요인이고 웃음이 청각 상품이라
  `no faces` / `no voice` 금지어를 뺐다 → 7-1절 신설. 말(spoken words)은 계속 금지한다
- 채널 시그니처인 **선택형 3클립**에 프루트 베이비를 얹어 트렌드 추종과 연작 원칙을 동시에 만족시켰다
- 프롬프트 이탈 없음. 3클립 모두 코스튬 아기 + 거대 과일로 나왔고 10초 내내 먹고 웃는다.
  **에이전트가 2번 클립을 큐에 8초로 걸었다가 실제로는 10초로 뽑았다** — 큐 표시는 믿지 말고 완성본 메타데이터를 본다
- 원본 -19.8 / -17.7 / -22.1 LUFS, LRA 4.4 / 1.9 / 5.1. **이 채널 소재 중 원본이 가장 크다**
  (유리 -28~-32, 젤리 -41~-46). 아기 웃음·먹는 소리가 크게 실린다

#### 18초 압축본 (규칙 #4 첫 적용)

- 30초 대신 **10초 클립에서 6초씩 잘라 18초**로 갔다. 배속이 아니라 컷이다 — 배속은 웃음 피치가 틀어진다
- 초 단위 `mean_volume`으로 가장 촘촘한 6초 창을 골랐다: s1 4.0~10.0, s2 2.0~8.0, s3 2.0~8.0.
  세 클립 다 앞 0.5초에 -100 dB대 무음 리드인이 있어 0초부터 쓰면 안 된다

#### 이 Mac의 ffmpeg에는 drawtext가 없다

- `brew` ffmpeg 9.0.1 빌드에 freetype이 안 들어가 있어 `No such filter: 'drawtext'` 로 죽는다.
- 재설치 대신 **PIL로 1080x1920 투명 PNG를 굽고 `overlay=0:0`** 으로 얹었다(`mktext.py`). PIL 12.3.0은 이미 있다
- 후크 텍스트 y=150/250, 씬 라벨 y=1380. **라벨을 y=1560에 두면 Shorts 하단 UI(제목·채널, 약 300px)에 가린다**

#### 오디오 체인 (규칙 9절 신규 규격 첫 적용)

```
highpass=f=45:p=2, loudnorm=I=-14:TP=-1.0:LRA=7, volume=${g}dB,
alimiter=limit=0.89:attack=5:release=60:level=disabled, afade in/out 0.04
```

- **`volume`은 반드시 `loudnorm` 뒤에 둔다.** 앞에 두면 loudnorm이 도로 정규화해 게인이 상쇄된다.
  앞에 뒀을 때 -13.2 / -14.5 / -13.2(편차 1.3 dB) → 뒤로 옮기고 `GAIN1=-0.8 GAIN2=0.5 GAIN3=-0.8` 로 **세 클립 모두 -14.0 LUFS(편차 0.0 dB)**
- 최종 -14.0 LUFS / TP -1.0 dBTP / LRA 2.3 / 18.02초 / 1080x1920 24fps
- 이 소재는 AAC 오버슈트가 작다. 리미터 천장 0.89 그대로 두고도 인코딩 후 피크 -1.0 ~ -1.8 dBFS

#### 업로드 함정

- **`concat -c copy` 로 만든 파일을 Studio가 거부했다** — "처리 중단됨 / 예기치 않은 문제가 발생했습니다".
  파일 자체는 ffmpeg 디코딩과 ffprobe를 정상 통과한다. `concat` 필터로 3클립을 **한 번에 재인코딩**하니 바로 올라갔다.
  숏폼은 길이가 짧아 재인코딩 비용이 없으니 처음부터 이쪽으로 만드는 편이 낫다
- 업로드 파일은 세션 스크래치패드로 복사해서 올렸다(바탕화면 경로는 거부당한다)
- 채널 진입은 `studio.youtube.com/channel/UCPIWmAeY05bOdFAa8OXyQfg` 로 바로 가면 계정 인덱스를 따질 필요가 없다
- 공개 상태 라디오는 이번에도 ref 클릭이 안 먹어 `document.querySelectorAll('tp-yt-paper-radio-button')[2].click()` 로 눌렀다
- 첫 댓글 고정 완료: `I'm taking the cherry baby home! 🍒 Which one are you picking? 👇`

### 2026-09-24 실행 기록 22 — 프루트 베이비 #2 (hoohihi123123, 렌더 실패로 2클립 12초)

- 결과물: https://youtube.com/shorts/Y8UshC-a_oY
- 트렌드 재확인: 프루트 베이비 1등 유지(Baby Giggles 1,304,818/7일). 포맷 유지하고 과일만 돌렸다
- 소재: 수박 · 오렌지 · 블루베리로 잡았으나 **블루베리가 2회 연속 실패**해 수박·오렌지 2클립으로 갔다

#### Flow 렌더가 죽는 두 가지 형태 (2026-09-24 확인)

1. **즉시 실패** — 카드에 "죄송합니다. 동영상을 생성할 수 없습니다." 가 뜬다. `↻` 재시도 한 번으로 복구되고 **크레딧이 나가지 않는다**(수박이 이 경우)
2. **99% 교착** — 진행률이 99%에서 멈춰 몇 분이 지나도 안 끝난다. 다운로드 버튼은 눌리지만 파일이 안 떨어지고,
   새로고침하면 **카드 자체가 사라진다.** 이쪽은 재시도분이 **과금된다**(블루베리 2회 중 2회차에서 15 크레딧 소모 확인: 20 → 5)

같은 프롬프트로 두 번 다 99%에서 죽었으니 소재별로 재현되는 문제로 본다. **한 소재가 99%에서 두 번 죽으면 그 소재는 버리고 다른 과일로 간다.**
크레딧이 모자라면 클립 수를 줄이는 편이 낫다 — 숏폼은 2클립 12초로도 성립한다

#### 계정 UI 차이 (hoohihi123123 = authuser=1)

- 이 계정은 **직접 생성 모드**다. 프롬프트 상자 우측 설정 칩(`동영상 · 720p · 8초 · 9:16 x1`)으로 바꾸며, `Generate exactly one...` 래퍼가 필요 없다
- 새 프로젝트 기본값이 **Veo 3.1 - Lite / 8초**였다. Omni 1.1 Flash로 바꾸면 **해상도가 360p로 리셋되니** 720p와 10초를 다시 눌러야 한다
- 새로고침 후 **첫 프롬프트 입력이 통째로 씹힌다.** 설정 패널을 한 번 열었다 닫고 다시 치면 들어간다
- "크레딧이 부족합니다" 배너는 잔량 20에서도 상시로 뜬다. 실제 생성은 그대로 진행된다

#### 빌드

- 2클립 × 6초 = 12초. 6초 창은 초당 `mean_volume` 측정으로 둘 다 2.0~8.0이 최적이었다
- 원본 -19.5 / -19.7 LUFS, LRA 4.0 / 8.2. 실행 기록 21과 마찬가지로 이 채널 소재 중 큰 편이다
- 기본값이 -14.1 / -13.4(편차 0.7 dB) → `GAIN2=-0.6` 으로 -14.1 / -14.0(편차 0.1 dB)
- 최종 -13.9 LUFS / TP -1.1 dBTP / LRA 4.5 / 12.00초 / 1080x1920 24fps / **5.1MB**
  (12초짜리는 CRF 20 단일 인코딩으로도 10MB 한도 안에 들어와 경량본이 필요 없다)
- 실행 기록 21 교훈대로 처음부터 `concat` 필터 단일 재인코딩으로 만들었다. Studio 가 한 번에 받았다

#### 업로드

- 해시태그를 치면 자동완성이 **`#shorts` 를 `#shortsfunny` 로 바꿔 넣는다.** 입력 후 스크린샷으로 확인하고,
  틀어졌으면 `cmd+Down` 으로 끝에 가서 백스페이스로 잘라낸다. 다이얼로그 빈 곳을 클릭해 드롭다운을 닫는다
- 첫 댓글 고정 완료: `I'm adopting the watermelon baby! 🍉 Which one are you taking? 👇`
